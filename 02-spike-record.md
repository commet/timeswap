# v0 기술 검증 기록

> 작성 2026-08-18. 공식 공개 데이터와 합성 학교만을 사용한 독립 기술 검증 기록이다.

## 1. 데이터 경로

학교·학급·교시·과목과 학사일정은 나이스 교육정보 개방 API에서 읽는다. 공개 자료에 없는
담당 교사는 학교가 직접 작성한 배정표로 보완한다. 다른 상용 서비스의 데이터, 코드,
비공식 API, 전용 파일 형식은 입력 경로로 두지 않는다.

## 2. 탐색 엔진

엔진은 빈 시간 이동, 두 수업 맞바꾸기, 세 수업 연쇄 교체를 전수 탐색한다. 분반,
복수교사, 합반은 하나의 이동 단위로 묶고 다음 하드 제약을 적용한다.

- 한 교사와 한 학급에 같은 교시 수업이 겹치지 않는다.
- 담당 미상 수업과 수업이 없는 요일을 빈 시간으로 사용하지 않는다.
- 휴업일과 요일별 존재하지 않는 교시로 이동하지 않는다.
- 학교가 지정한 교사 근무 불가 시간으로 이동하지 않는다.

후보는 변경 수, 같은 학급을 하루 두 번 만나는지, 연속 수업과 하교 지연, 최근 협조
부담을 점수화하고 사람이 읽을 수 있는 근거를 함께 돌려준다.

## 3. 웹 검증

웹은 시간표 불러오기, 교사 선택, 후보 비교, 요청, 승인·반려, 행정 체크와 게시,
학급 시간표, 공지·나이스 입력 목록·결재 문서 생성을 지원한다. 현재 자료는 브라우저에
저장하며 서버를 사용하지 않는다.

## 4. 다음 검증

1. 학교가 권한을 갖고 제공한 실제 파일로 입력 경로를 검증한다.
2. 학교별 인증·격리·보존·파기를 갖춘 공유 저장소를 연결한다.
3. 현직 교사와 일과 담당자의 실제 업무 순서로 사용성을 검증한다.
