# v0 알파 스파이크 기록

> 작성 2026-08-16. `01-plan.md`의 W1, W2 스파이크를 하루에 압축 실행한 기록.
> 결과물: `packages/engine`(엔진), `apps/web`(웹 앱), `scripts/smoke.mjs`(스모크 테스트), Vercel 배포.

## 1. 데이터 경로의 반전: 엑셀 대신 뷰어 JSON

계획(01-plan 6장)은 컴시간 엑셀 업로드를 진입 경로로 잡았다. 실측 결과가 계획을 바꿨다.

- 확인된 사실: 컴시간알리미 리메이크 뷰어(timetable.s2h9.dev)가 학교 코드 하나로
  전교 시간표를 JSON으로 받아 온다. `GET /api/getalltimetable?schoolCode=...&period=1|2`
  응답에 학년 > 반 > 셀 구조로 교사명, 과목, 요일, 교시가 들어 있고, **`isChanged` 와
  `changeInfo` 로 학기 중 결보강 변경분까지 실시간 반영**되어 있다. 교사명은 소스에서
  마스킹("이수*")되어 온다.
- 시사점 1: "이번 주에 실제로 돌아가는 시간표"를 얻는 데 엑셀이 필요 없다. 교사가
  엑셀을 못 내려받는 경우(일과진행 담당이 아닌 일반 교사)에도 진입이 된다.
- 시사점 2: 리서치(00-research 2.4)의 결론대로 비공식 파싱을 우리 서버로 하는 것은
  차단, 약관 리스크가 있다. v0 알파는 **JSON 파일 업로드와 내장 샘플**로 시작하고,
  학교 코드 직접 조회(서버 프록시)는 별도 리스크 결정으로 남긴다.
- 산출물: `packages/engine/src/adapters/comcigan.ts` (셀 좌표를 배열 인덱스 대신 셀 안의
  weekday, classTime 값으로 읽어 서식 변형에 강하다. 같은 교사가 같은 교시에 두 학급에
  잡히면 동시수업 묶음으로 마킹해 추천에서 정직하게 제외한다.)

## 2. 엔진 실측

테스트 22개 전부 통과 (`npm test`).

| 검증 | 내용 | 결과 |
|---|---|---|
| 골든 6 | 손으로 만든 학교에서 수작업 정답과 일치 (맞교환 유일 최선, 이동 최선, 분반 제외) | 통과 |
| 속성 12 | 무작위 학교 12개 x 결강 3건, 모든 추천안이 하드 제약 보존, 중간 공강 불증가 | 통과 |
| 실데이터 3 | 실제 고교(41학급, 교사 78명, 수업 1,312개) 어댑터와 추천 안전성 | 통과 |
| 성능 1 | 실제 규모 결강 10건 각각 1초 예산 | 통과, 결강 1건 약 13ms |

- 설계 변경: 이동(moveToEmpty) 규칙을 "그날 마지막 교시만"에서 "학급의 중간 빈 교시가
  늘지 않으면 허용(기존 빈틈 메우기 포함)"으로 일반화했다. 실데이터에 중간 공강이
  실재해서다.
- 합성 생성기는 MRV 휴리스틱으로도 학급 30개 규모에서 교착이 잦아, 성능 검증을
  실데이터로 바꿨다. 합성은 속성 테스트용 소규모(학급 12)로만 쓴다. 대규모 합성이
  필요해지면 이분 그래프 간선 채색 관점의 생성기로 다시 만든다.

## 3. 웹 v0 알파

- 흐름: 샘플 학교 즉시 체험 또는 JSON 업로드 → 교사 주간 격자 → 결강 셀 클릭 →
  추천 카드(근거 트레이스, 변경 규모) → 카드 호버 시 격자에 변경 전후 미리보기 →
  합쇼체 요청 문구 복사. 시간표는 localStorage 에만 저장(데이터 무전송 원칙).
- 검증: `scripts/smoke.mjs` (Playwright, 프로덕션 빌드 대상). 랜딩, 샘플 로드, 결강
  지정(추천 12건), 미리보기 표시, 문구 복사(클립보드 내용까지 확인), 교사 전환(78명),
  새로고침 복원, 모바일 뷰까지 통과. 페이지 오류 0.
- 디자인: 리서치 아티팩트의 시각 언어(종이, 잉크, 칠판 초록, 정정 주황)를 제품
  토큰으로 옮겼다. 과목 색은 이름 해시로 hue 만 정하고 채도, 명도는 테마가 정한다.
  다듬은 것: 원점수(-25점) 노출을 "수업 n개 이동"으로 교체, 동시수업 기호를 글꼴
  독립적인 칩으로 교체. impeccable 원칙(순수 회색 금지, 카드 중첩 금지, 목적 있는
  단일 모션)을 반영했고 정적 탐지에서 지적 0건. URL 모드 탐지는 샌드박스 루트 계정
  제약으로 다음 기회에 돌린다.

## 4. 배포

- Vercel 프로젝트 `timeswap` 을 commet/timeswap 저장소(루트 apps/web)에 연결.
  MCP 토큰 권한이 배포 생성과 조회를 막아, 배포는 GitHub 연동 푸시로 트리거했다.
  빈 커밋은 변경 파일이 없어 빌드가 취소된다는 것을 확인했다(실변경 커밋 필요).
- `timeswap.vercel.app` 은 동명의 타사 프로젝트가 선점한 도메인이다. 우리 URL은
  Vercel 이 부여한 팀 스코프 도메인을 쓰고, 정식 도메인은 제품명 확정과 함께 정한다.

## 5. 다음 단계 (우선순위 순)

1. 사용자 본인 학교 데이터로 실사용 검증(JSON 확보 경로 안내 포함)과 동료 교사 테스트.
2. 학교 코드 직접 조회의 리스크 결정: 자체 프록시로 갈지, 파일 업로드 유지로 갈지.
3. 결강 다중 지정(하루 결근)과 보강 후보 추천(01-plan P2 시나리오).
4. 수업교체 계획서 인쇄(A4)와 학급 뷰.
5. 엑셀 업로드 지원(교무실 관행 대응, 실물 서식 확보 후).
