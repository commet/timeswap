# timeswap 제품과 구현 계획 v1

> 작성 2026-08-16. 근거: `00-research.md` (딥 리서치 v1). 다음 문서: `02-*` (스파이크 기록).
> 전제: 만드는 사람이 교단에서 직접 겪은 페인 포인트가 요구사항의 1차 소스다. 리서치는
> 그 경험이 일반적임을 확인했다(규정 원문, 교사 증언, 해외 제품 공백). 목표는 자기용
> 도구가 아니다. 전국의 교사가 부담 없이 쓰는 도구다.

---

## 0. 제품 한 줄과 승부처

> **결강이 생겼을 때, 성립하는 교환안을 근거와 함께 몇 초 안에 보여준다.**

리서치가 가른 승부처는 세 가지다. 알고리즘 난도는 승부처가 아니다.

1. **진입 마찰 제로.** 컴시간을 버릴 필요도, 가입할 필요도, 설치할 필요도 없어야
   전국 단위 확산이 성립한다.
2. **설명 가능한 추천.** 컴시간 자동 교환의 실패 원인은 "왜 이렇게 바꾸는지 모르겠고
   필요 이상으로 많이 바꾼다"였다. 근거 없는 추천은 교무실에서 신뢰받지 못한다.
3. **아름답고 명료한 격자.** 교사의 멘탈 모델은 시간표 격자다. 격자가 곧 인터페이스가
   되어야 배울 것이 없는 도구가 된다.

## 1. 제품 원칙 (설계 헌장)

1. **현장이 정의한 문제만 푼다.** 기능 추가의 근거는 언제나 교단의 실제 상황이다.
   상상 속 관리자 기능을 먼저 만들지 않는다.
2. **첫 가치까지 3분.** 엑셀을 끌어다 놓으면 격자가 뜨고, 셀을 짚으면 교환안이 나온다.
   회원가입은 v0에 존재하지 않는다.
3. **v0은 데이터 무전송.** 파싱과 탐색을 전부 브라우저 안에서 한다. 시간표가 서버로
   나가지 않으므로 개인정보 부담이 0이고, 학교 보안 검토 없이 교사 개인이 바로 쓸 수
   있다. "전국의 교사 누구나"의 기술적 실체가 이것이다.
4. **추천에는 반드시 근거.** 모든 추천안은 통과한 조건과 감점 내역을 사람 말로 함께
   보여준다. 점수만 있는 랭킹은 금지.
5. **격자가 인터페이스.** 별도의 입력 폼 대신, 격자의 셀을 직접 짚어서 결강을
   지정하고, 추천안 위에 마우스를 올리면 격자에 변경 전후가 미리 보인다.
6. **아름다움은 명료함이다.** 과목 색 체계, 변경 diff의 시각화, 인쇄물의 결까지
   하나의 시각 언어로 만든다. 장식이 목적인 요소는 두지 않는다.
7. **화면 문구는 합쇼체.** 표기 규칙은 `CLAUDE.md`를 따르고, 렌더링 검사로 잠근다.

## 2. 사용자와 핵심 시나리오

### P1. 교과 교사 (v0의 주인공)

> 다음 주 화요일 3교시, 출장이 잡혔다.

1. 컴시간에서 내려받아 두었던 시간표 엑셀을 timeswap에 끌어다 놓는다. (최초 1회)
2. 내 시간표 격자에서 화요일 3교시 셀을 짚고 "이 수업을 옮깁니다"를 누른다.
3. 교환안 목록이 뜬다. 맨 위 안: "목요일 2교시 김OO 선생님과 맞교환. 수업 2개만
   움직입니다." 근거 카드에 통과 조건과 감점 내역이 보인다.
4. 안을 고르면 상대 교사에게 보낼 요청 문구가 만들어진다. 복사해서 메신저로 보낸다.
5. 수락 답장을 받으면 "수업교체 계획서" 버튼으로 학교 서식 계열의 인쇄물을 뽑아
   결재를 올린다.

### P2. 일과계(수업계) 담당 교사

> 아침에 병가 연락이 두 건 왔다.

1. 두 교사의 오늘 수업을 결강으로 지정한다.
2. 서로 겹치지 않는 교환 조합을 추천받는다. 교환이 안 되는 시간만 보강 후보(공강
   교사, 과목 일치 우선, 최근 보강 횟수 균형)가 뜬다.
3. 확정 내역이 결보강 기록 초안으로 쌓인다. (v0은 개인 기록, v1에서 학교 공유)

### P3. 교무부장 (v1 이후)

월별 결보강 통계, 교사별 부담 공정성, 수당 집계를 본다. v0에서는 만들지 않는다.

## 3. 범위 사다리

| 단계 | 대상 | 담는 것 | 담지 않는 것 |
|---|---|---|---|
| **v0 (4주)** | 교사 개인, 무료 | 엑셀 업로드와 서식 보정, 주간 격자, 결강 지정, 교환 추천(2자, 3자, 빈 교시 이동), 근거 카드, diff 미리보기, 요청 문구, 교체 계획서 인쇄, 브라우저 저장 | 계정, 서버 저장, 학교 공유, 보강 배정, 통계 |
| **v0.5 (2주)** | 위와 같음 | 타 학교 엑셀 서식 커버리지, 분반과 동시수업 표현, 보강 후보 추천, 현장 피드백 반영 | |
| **v1 (8주)** | 학교 구독 | Supabase 계정과 학교 워크스페이스, 공유 시간표와 변경 이력(기록 시스템화), 결보강 기록부, 공정성 카운터, 수당 집계, 학교장터(S2B) 입점 | |
| **v2** | | 자연어 요청(LLM 입력 계층), 다중 결근 최적화(CP-SAT), 고사감독 배정, 알림 | |

v1 착수 게이트: 주간 활성 교사 100명, 또는 학교 단위 도입 문의 3건. 게이트 전에는
v0의 완성도에만 투자한다.

## 4. 화면 설계

### 4.1 정보 구조

```
시작(업로드) → 격자(주간)
                ├─ 셀 선택 → 추천 패널(우측) → 안 상세 → 요청 문구 | 계획서 인쇄
                ├─ 뷰 전환: 내 시간표 | 학급 | 교사
                └─ 설정: 이름 매핑, 제약(근무 불가 시간), 가중치
```

### 4.2 화면 다섯 개

1. **시작.** 화면 중앙에 드롭존 하나. "컴시간에서 내려받은 시간표 파일을 끌어다
   놓으십시오." 아래에 "샘플 시간표로 둘러보기" 버튼을 두어, 파일이 없어도 3초 안에
   제품 가치를 만져 볼 수 있게 한다.
2. **서식 확인.** 파싱 결과를 "이렇게 읽었습니다"로 보여주고 교사명, 과목, 학급의
   매핑을 표에서 바로 고친다. 파서 신뢰도가 낮은 칸은 표시가 붙는다. 이 화면이
   파서의 절반이다.
3. **격자.** 주간 그리드. 과목별 저채도 색 토큰(색약 안전 12색), 오늘 열 강조,
   숫자는 tabular-nums. 셀 클릭이 곧 작업의 시작이다.
4. **추천 패널.** 우측 슬라이드. 카드마다 유형 배지(맞교환 | 3자 순환 | 이동),
   움직이는 수업 수, 점수, 관련 교사. 카드에 마우스를 올리면 격자에 변경 전후가
   겹쳐 보인다(원래 자리는 흐려지고, 옮겨 갈 자리에 화살표와 하이라이트).
   맨 아래에 "교환이 안 되는 이유" 접기 항목: 탈락한 대표 후보 몇 개와 탈락 사유를
   보여줘서 도구가 성실히 탐색했음을 증명한다.
5. **안 상세와 산출물.** 절차 안내(누구에게 무엇을 부탁하는지), 합쇼체 요청 문구와
   복사 버튼, 확정 시 A4 인쇄 최적화된 수업교체 계획서(결재란, 변경 전후 표 포함).

### 4.3 시각 언어

- 리서치 아티팩트의 팔레트를 계승한다: 종이 바탕, 잉크 텍스트, 칠판 초록(교환,
  성립), 정정 주황(결강, 보강). 의미 색과 장식 색을 구분한다.
- 글꼴: Pretendard 셀프 호스팅(제품에서는 웹폰트 제약이 없다), 본문 15px 이상,
  줄 간격 1.7 이상. 다크 모드는 토큰 교체로 지원한다.
- 모션: diff 미리보기 전환 200ms 이하 한 곳에 집중, `prefers-reduced-motion` 존중.
- 모바일: 조회와 요청 문구 공유에 최적화한 읽기 뷰. 편집과 탐색은 데스크톱 우선.

## 5. 기술 아키텍처

### 5.1 스택

| 층 | 선택 | 이유 |
|---|---|---|
| 프런트 | Next.js(App Router) + TypeScript + Tailwind | 정적 우선, Vercel 배포, 팀의 기존 스택 |
| 파서 | SheetJS(xlsx) + 자체 서식 추론기 | 컴시간 출력 서식 대응, 브라우저 내 처리 |
| 엔진 | 프레임워크 무관 TS 패키지 `@timeswap/engine` | UI와 분리, CLI와 테스트에서 재사용 |
| 저장 | IndexedDB(+ JSON 내보내기, 가져오기) | v0 데이터 무전송 원칙 |
| v1 백엔드 | Supabase(Auth, Postgres RLS, Realtime) + Vercel | 학교 워크스페이스, 변경 이력 |
| v2 최적화 | OR-Tools CP-SAT(서버 함수, Python) | 다중 결근 서로소 조합 등 나열 폭발 구간 |
| v2 LLM | Claude API | 자연어 요청 해석과 근거 문장 다듬기. 판정은 항상 엔진 |

### 5.2 엔진 설계

```
모델
  Slot        요일 x 교시 (주 35칸, 학교 설정으로 가변)
  Assignment  { teacher, klass, subject, slot, group? }   // group: 분반, 동시수업 묶음
  Timetable   불변 구조. 파생 인덱스: 교사별, 학급별 비트마스크(BigInt 35비트)
  Move        { type: 이동 | 맞교환 | 3자순환, changes: Assignment 변경 목록 }

탐색 (결강 슬롯 s, 교사 t)
  1) moveToEmpty:  같은 학급의 빈 교시로 이동
  2) swap2:        같은 학급에 수업이 있는 교사 u와 맞교환
  3) cycle3:       같은 학급 세 수업의 회전 (양방향)
  → 하드 필터: 교사 중복 배정 금지, 근무 불가 시간, 학급 공백 금지, 분반 묶음 보존
  → 소프트 점수로 정렬, 각 후보에 트레이스(통과 조건, 감점 내역) 부착

소프트 점수 초기 가중치 (설정으로 조정 가능)
  움직이는 수업 수            건당 -10
  동일 과목 하루 2회           -6
  상대 교사 공강 파괴           -4
  연강 생성 또는 파괴           -3
  결강 당일 해결               +5
  최근 보강, 교환 부담 균형     +-3
```

- 성능 예산: 후보 30만 개(3자 순환 전수) 평가를 **Web Worker에서 1초 안**. 리서치
  5장의 추정으로 여유가 있고, 모자라면 후보 생성기를 학급 단위로 지연 평가한다.
- 설명 생성: 트레이스를 템플릿 문장으로 바꾼다. v0에 LLM은 없다. 결정론이 신뢰의
  기반이고, 비용이 0이어야 무료 배포가 성립한다.
- 불변식: **모든 추천안은 적용 후에도 하드 제약을 전부 만족한다.** 속성 테스트로
  잠근다.

### 5.3 테스트 전략

| 검사 | 대상 |
|---|---|
| 골든 테스트 | 합성 시간표 + 수작업 정답 교환안. 리서치의 규정 시나리오를 케이스로 옮김 |
| 속성 테스트 | 무작위 시간표에서 모든 추천안의 하드 제약 보존, 교환 적용의 가역성 |
| 파서 스냅샷 | 학교별 엑셀 실물마다 파싱 결과 고정 |
| 표기 규칙 | fsk-ops의 렌더링 실측 검사를 이식(합쇼체, em 대시와 중간점 금지) |
| 성능 | 표준 규모(교사 60, 학급 30) 전수 탐색 1초 예산 회귀 검사 |

## 6. 데이터 입수 (선결 과제)

- **W1 최우선: 컴시간 엑셀 실물 확보.** 전체 시간표, 교사별, 학급별 3종. 본인 학교
  1부에서 시작해 서식 변형을 수집한다. 리서치 10장의 미해결 질문 1번이 이것이다.
- 파서는 "완벽 자동 인식"을 목표하지 않는다. 추론 + 확인 화면 + 수동 보정의 조합이
  현실적 목표다. 보정 결과는 저장해서 다음 업로드에 재사용한다.
- 분반, 동시수업, 강사 시간처럼 엑셀에 안 드러나는 제약은 v0에서 정직하게 다룬다:
  인식 못 한 묶음은 경고와 함께 해당 후보를 제외하고, 설정에서 수동 지정할 수 있게
  한다.

## 7. 구현 로드맵

| 주 | 산출물 | 완료 판정 |
|---|---|---|
| **W1** | 엑셀 실물 3종, 파서 스파이크, 엔진 코어(탐색 + 점수 + 트레이스), CLI, 골든 테스트 5건 | CLI로 실데이터에서 교환안 상위 5개와 근거가 나온다 |
| **W2** | 격자 UI, 셀 선택 결강 플로우, 추천 패널과 diff 미리보기 | 알파: 업로드부터 추천 열람까지 이어진다 |
| **W3** | 서식 확인 화면, 근거 카드, 요청 문구, 계획서 인쇄, 빈 상태와 오류 문구(합쇼체), 표기 검사 이식 | 첫 가치 3분 시나리오가 끊김 없이 돈다 |
| **W4** | 현장 테스트(본인 + 동료 교사 5인), 수정, 무료 공개, 소개 페이지 | 외부 교사가 안내 없이 첫 추천까지 도달한다 |
| **W5, W6 (v0.5)** | 타 학교 서식 커버리지, 분반과 동시수업, 보강 후보 추천 | 서식 실패율이 수집 표본에서 1할 아래로 |

- 리서치 9장의 검증 관문과의 관계: 만드는 사람이 교단의 1차 증언자이므로 "문제 실재"
  관문은 통과로 본다. 남은 검증(일반화, 지불 의사)은 W4 현장 테스트와 공개 후 지표로
  대체한다. 도구를 들고 하는 인터뷰가 설문보다 정확하다.
- v1(학교 구독, 8주)은 게이트 통과 후 별도 계획 문서로 상세화한다. 골자는 3장의 표와
  리서치 6, 7장(S2B, 가격 20만에서 30만 원대, WebUntis 구도)을 따른다.

## 8. 성공 지표 (v0)

| 지표 | 정의 | 목표 |
|---|---|---|
| 첫 가치 도달률 | 업로드 시작 대비 첫 추천 열람 | 7할 |
| 첫 가치 시간 | 업로드에서 첫 추천 열람까지 | 3분 이내 |
| 추천 채택 행동 | 요청 문구 복사 또는 계획서 인쇄 | 세션의 3할 |
| 재사용 | 2주 내 재방문(결강은 반복 사건) | 4할 |

측정은 무전송 원칙과 충돌하지 않게 익명 이벤트 최소 수집(행동 4종)만 하고, 시간표
내용은 어떤 형태로도 보내지 않는다. 수집 항목은 화면에 공개한다.

## 9. 구현 리스크와 완화

| 리스크 | 내용 | 완화 |
|---|---|---|
| 엑셀 서식 다양성 | 학교마다 출력 옵션이 달라 파서가 깨진다 | 확인과 보정 화면을 1급 기능으로. 실패 서식은 익명 제보 버튼으로 수집 |
| 숨은 제약 | 분반, 동시수업, 특별실이 엑셀에 안 보인다 | 인식 못 하면 후보 제외와 경고. 수동 지정 UI. v0.5에서 정식화 |
| 이름 표기 | 동명이인, 성만 표기, 존칭 변형 | 매핑 화면에서 확정, 저장 후 재사용 |
| 모바일 격자 밀도 | 35칸 격자가 좁은 화면에 안 들어간다 | 모바일은 읽기와 공유 전용 뷰로 분리 |
| 컴시간 재입력 이중화 | 확정안을 컴시간에 다시 넣어야 알리미에 반영 | 계획서와 변경 요약으로 재입력을 1분 작업으로. v1에서 기록 시스템 지위 확보 |

## 10. 이 저장소의 다음 산출물

1. `02-spike-parser.md` 컴시간 엑셀 실물 분석과 파서 스파이크 기록
2. `02-spike-engine.md` 엔진 코어와 골든 테스트 기록
3. `packages/engine/` 첫 코드 (W1)
4. `apps/web/` Next.js 앱 (W2)

## 11. 오픈 퀘스천

- 제품명. timeswap은 가칭이다. 한국 교사 대상 네이밍(한글 이름 포함)을 W3 전에 정한다.
- 무료의 선. v0은 전부 무료. v1에서 개인 무료 범위(무제한 vs 학교 기능만 유료)를
  가격 실험과 함께 정한다.
- v1 교사 인증. 학교 메일이 없는 교사가 많다. 워크스페이스 초대 링크 방식이 유력.
- 컴시간 외 입력원. 나이스 개방 API(기초시간표)를 보조 입력으로 붙일지 v0.5에서 판단.
