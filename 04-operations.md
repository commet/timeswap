# 04. 운영 구성

작성일 2026-08-16. 많은 교사가 쓰게 될 물건이라 배포와 검증을 흔들리지 않게 못 박는다.
"고쳤다"가 아니라 "다시 깨질 수 없다"가 목표다.

---

## 1. 배포 구조

```
GitHub (commet/timeswap, main)
   │  푸시
   ├─ GitHub Actions 검증  ← 여기서 막히면 배포까지 가지 않는다
   └─ Vercel 빌드 → 정적 파일 → CDN
```

서버가 없다. Next.js 정적 내보내기라 결과는 HTML, CSS, JS 뿐이고 CDN 이 그대로 내보낸다.
데이터베이스도, 로그인도, 우리 쪽 API 도 없다. 고장 날 수 있는 부품이 그만큼 적다.

### Vercel 설정을 두 벌 두는 이유

Vercel 의 루트 디렉터리 설정은 대시보드에 있고 저장소가 정할 수 없다.
어느 쪽으로 잡혀 있어도 맞게 돌도록 설정 파일을 두 곳에 같은 내용으로 둔다.

| 루트 디렉터리 설정 | 읽는 파일 | 출력 위치 |
|---|---|---|
| 저장소 루트 | `vercel.json` | `apps/web/out` |
| `apps/web` | `apps/web/vercel.json` | `out` |

이전 배포가 깨진 이유가 이 자리였다. 정적 내보내기 결과는 `out` 에 나오는데
Vercel 은 기본값인 `public` 을 찾고 있었다. 빌드는 성공하고 마지막 한 줄에서 멈추는 모양이라
로그를 끝까지 봐야 보인다.

지금 실제로 걸려 있는 값은 `apps/web` 이다. 2026-08-16 배포 알림이 `rootDirectory: apps/web`
을 그대로 실어 보내 확인했다. 즉 현재 읽히는 파일은 `apps/web/vercel.json` 한 벌이다.
그래도 두 벌을 같은 내용으로 유지한다. 대시보드 설정은 저장소 밖에 있어 언제 바뀌었는지
알 방법이 없고, `npm run check` 가 두 벌의 헤더가 어긋나면 관문에서 잡는다.

설치는 `npm install` 이 아니라 `npm ci` 를 쓴다. 잠금 파일과 어긋나면 조용히 다른 버전을 깔지 않고
소리 내어 멈춘다. 배포에서는 그쪽이 안전하다.

### 응답 헤더

정적 파일이라 서버에서 할 수 있는 방어가 헤더뿐이다. 다음을 건다.

| 헤더 | 값과 뜻 |
|---|---|
| Content-Security-Policy | 스크립트는 우리 것만, 연결은 나이스 개방 포털만 허용 |
| X-Frame-Options | DENY. 남의 화면에 끼워 넣지 못한다 |
| X-Content-Type-Options | nosniff |
| Referrer-Policy | strict-origin-when-cross-origin |
| Permissions-Policy | 카메라, 마이크, 위치를 모두 끈다 |
| Cache-Control | `_next/static` 은 1년 고정 캐시 |

CSP 는 설정만 보고는 앱이 도는지 알 수 없다. 그래서 `scripts/serve-checked.mjs` 가
`vercel.json` 의 헤더를 그대로 읽어 씌우고, 그 위에서 전 과정 점검을 돌린다.
설정과 검사가 같은 원본을 보므로 한쪽만 바뀌어 어긋날 수 없다.

---

## 2. 검증 관문

`.github/workflows/ci.yml` 이 푸시와 풀 리퀘스트마다 돈다.
**싼 검사를 앞에 둔다.** 몇 초짜리 검사가 잡을 잘못 때문에 십 분짜리 빌드를 기다릴 이유가 없다.

| 순서 | 단계 | 걸리는 시간 | 무엇을 잡는가 |
|---|---|---|---|
| 1 | `npm ci` | 중 | 잠금 파일 어긋남 |
| 2 | `npm run check` | 1초 미만 | 배포 설정 어긋남 |
| 3 | `npm run typecheck` | 십여 초 | 엔진과 웹 앱 타입 |
| 4 | `npm test` | 수 초 | 엔진 시험 70개와 웹 lib 시험 17개 |
| 5 | 브라우저 준비 | 저장분이 있으면 짧음 | |
| 6 | `npm run build` | 중 | 정적 내보내기 |
| 7 | 정적 출력 존재 확인 | 1초 미만 | 결과물이 실제로 나왔는지 |
| 8 | 브라우저 전 과정 점검 | 중 | 화면이 실제로 도는지 |
| 9 | 화면 갈무리 보관 | | 실패했을 때 눈으로 볼 것 |

취약점 점검은 따로 떼어 나란히 돌린다. 결과를 기다릴 이유가 없고,
외부 서비스에 기대는 단계라 본 관문의 실패 원인에 섞이면 진단이 흐려진다.

**8번이 핵심이다.** 단위 시험이 다 통과해도 화면이 안 뜨는 일이 있어서,
실제 브라우저로 시간표 불러오기부터 결강 지정, 추천 확인, 반영, 되돌리기,
문구 복사, 파일 저장, 테마 전환, 모바일 화면까지 한 번에 훑는다.
배포와 같은 응답 헤더를 씌워 띄우므로 CSP 가 자원을 막으면 여기서 실패로 잡힌다.

**빠르게 유지하는 장치 둘.**

- 브라우저는 판이 같으면 다시 내려받지 않는다. `playwright` 판을 열쇠로 저장분을 쓴다.
  같은 판에서는 의존 꾸러미만 다시 깐다.
- `npm` 저장분은 `setup-node` 가 맡는다.

### 정적 내보내기 사고를 다시 내지 않으려고

배포가 마지막에 `No Output Directory named "public"` 으로 죽은 일이 있었다.
빌드는 성공했는데 결과물을 찾는 자리가 달랐기 때문이다.
이런 것은 사람의 기억으로 막을 일이 아니라서 검사로 옮겼다.

`npm run check`(`scripts/check-config.mjs`)가 네 가지를 본다.

1. 웹 앱이 여전히 정적 내보내기인가(`output: 'export'`).
2. `vercel.json` 두 벌이 각자 자리에 맞는 출력 경로를 가리키는가.
3. 두 벌의 응답 헤더가 완전히 같은가.
4. 보안 헤더 다섯이 다 있고 CSP 에 나이스 출처가 살아 있는가.

`output: 'export'` 를 지우면 2번이 아니라 1번에서 걸린다.
어느 쪽을 고치든 짝을 함께 고치라고 말해 주는 것이 이 검사의 목적이다.

### 남은 손질

`main` 브랜치 보호는 저장소 설정이라 여기서 걸 수 없다. 다음을 켜기를 권한다.

- 검증 통과를 병합 필수 조건으로
- 직접 푸시 금지, 풀 리퀘스트 경유

---

## 3. 의존성 관리

- Node 는 `.nvmrc` 와 `engines` 로 22 로 못 박는다. 개발, CI, 배포가 같은 버전을 쓴다.
- `dependabot.yml` 이 매주 월요일에 갱신 제안을 올린다. 개발 도구와 런타임을 나눠 묶는다.
- 갱신은 CI 를 통과한 것만 받는다.

2026-08-16 에 취약점 8건(치명 1, 높음 4, 보통 3)을 0 으로 만들었다.

| 대상 | 조치 | 판단 |
|---|---|---|
| vitest 2 → 4 | 올림 | 치명 등급이 여기 있었다. 시험 49개 그대로 통과 |
| next 15 → 16 | 올림 | 높음 3건이 postcss 와 sharp 에서 왔다. 빌드와 전 과정 점검 통과 확인 후 반영 |

메이저 상향은 검증 없이 하지 않는다. 두 건 모두 시험, 빌드, 브라우저 점검을 통과한 뒤에 넣었다.

### 워크플로 액션의 Node 20 종료

CI 로그에 이런 경고가 남고 있었다. 지금은 통과하지만 언젠가 실패로 바뀐다.

```
Node.js 20 is deprecated. The following actions target Node.js 20 but are being
forced to run on Node.js 24: actions/cache@v4, actions/checkout@v4,
actions/setup-node@v4, actions/upload-artifact@v4
```

판 번호를 확인할 수 없는 자리에서 작업하고 있었다. 그래서 짐작으로 적는 대신
**CI 에게 물었다.** 없는 판을 적으면 `Set up job` 단계에서 몇 초 만에 죽으므로
확인 비용이 가장 싸다. 짐작으로 밀어 넣는 것과 다른 점은 틀렸을 때 즉시 드러난다는 것이다.

두 번에 걸쳐 재고 다음을 얻었다.

| 액션 | 올린 판 | 결과 |
|---|---|---|
| `actions/checkout` | v5 | Node 24. 경고 사라짐 |
| `actions/setup-node` | v5 | Node 24. 경고 사라짐 |
| `actions/cache` | v5 | Node 24. 경고 사라짐 |
| `actions/upload-artifact` | v5 로는 부족, **v6** | v5 도 Node 20 이었다. v6 에서 사라짐 |

경고가 로그에서 통째로 없어졌다. 넷을 뭉뚱그려 "최신으로 올린다"고 적어 두었더라면
`upload-artifact` 만 남는 것을 몰랐을 것이다. **한 번에 다 올리고 남는 경고를 읽는 편이
저장소를 하나씩 찾아보는 것보다 빨랐다.**

---

## 4. 자료를 어디에 두는가

### 현행 제품은 어떻게 하고 있는가

우리도 곧 같은 문제를 만나므로 먼저 그쪽 구조를 정리한다.
공개된 설치 안내와 사용 안내를 읽어 확인한 범위다.

**중앙 데이터베이스가 제품의 중심이 아니다.** 구조는 설치형이다.
학교 PC 에 프로그램을 깔고, 교내 서버 프로그램 하나가 상시 돌면서 회원 등록부와 자료를 중계한다.
학교 시간표 원본은 그 학교 안에 있다. 여러 학교의 시간표를 한 데이터베이스에 모아 두고
학교가 접속해 쓰는 형태가 아니다.

이 구조의 성질은 이렇다.

| 항목 | 그쪽 구조 | 결과 |
|---|---|---|
| 자료 위치 | 학교 안 PC | 학교 밖으로 시간표가 안 나간다 |
| 인증 | 교내 서버의 회원 등록부 | 계정 체계가 학교 안에 갇힌다 |
| 계정 발급 | 일과 담당이 등록 | 학교가 명부를 통제한다 |
| 판매 | 학교 단위 연간 사용권 | 계정 수와 무관하다 |
| 가용성 | 그 PC 가 꺼지면 멈춤 | 상시 가동 PC 가 필요하다 |
| 접근 경로 | 윈도우 클라이언트 | 방화벽을 열어야 하고 휴대폰으로는 못 본다 |

**열람용 웹은 완전히 별개다.** 학생과 학부모가 보는 알리미 웹은 학교를 고르면
로그인 없이 시간표가 나온다. 공개가 전제인 화면이라 그 자체로 결함은 아니지만,
누구나 아무 학교의 시간표를 볼 수 있다는 지적이 따라붙는다.
2018년에 그 응답을 받아 쓰던 외부 앱들이 배포를 멈춘 이력도 여기서 나왔다.

여기서 우리가 가져올 것과 버릴 것이 갈린다.

- **가져올 것.** 학교를 경계로 삼는 발상. 자료가 학교 밖으로 안 나가는 것이
  영업에서 실제로 힘이 된다. 우리는 그것을 브라우저 경계로 한 단계 더 좁혔다.
- **버릴 것.** 상시 가동 PC 와 윈도우 의존. 그쪽의 급소가 그대로 여기다.
  우리가 서버를 붙일 때 이 형태를 다시 만들면 얻는 것 없이 약점만 물려받는다.

### 지금 붙이지 않는 이유

Supabase 는 연결되어 있고 언제든 쓸 수 있다. 그런데도 지금은 붙이지 않는다.
미룬 것이 아니라 지금 붙이면 제품이 나빠지기 때문이다.

**첫째, 지금 구조의 가장 큰 강점을 잃는다.** 시간표가 브라우저 밖으로 나가지 않으므로
학교 보안 검토 없이 교사 개인이 그냥 쓸 수 있다. 서버에 담는 순간 이 문장을 못 쓴다.

**둘째, 개인정보가 된다.** 교사 이름과 시간표를 서버에 두면 처리방침, 수집 동의,
위탁 계약이 따라온다. 교사 한 사람이 오후에 결강 처리를 하려고 쓰는 도구에 붙일 무게가 아니다.

**셋째, 고장 날 곳이 는다.** 접속 장애, 인증 오류, 이관 실패, 행 수준 보안 설정 실수가
모두 새 실패 지점이다. 오류를 없애자는 목표와 정면으로 어긋난다.

**넷째, 지금 없어도 다 된다.** 불러오기, 탐색, 반영, 되돌리기, 인쇄, 파일 저장이 모두 돌아간다.

### 언제 붙이는가

세 가지 가운데 하나라도 실제로 요구되면 그때가 시점이다.

1. **학교 단위 교사 배정 공유.** 나이스 개방 자료에는 교사가 없어 사람이 채워야 한다.
   한 사람이 한 번 채운 것을 같은 학교 동료가 쓸 수 있으면 마찰이 크게 준다.
   이것이 가장 먼저 올 요구다.
2. **일과 담당자 절차.** 교환 요청과 승인, 나이스 반영 기록을 여럿이 함께 봐야 할 때.
3. **품앗이 균형의 학교 확장.** 지금은 한 브라우저 안에서만 센다.

### 붙일 때의 설계

미리 정해 둔다. 급할 때 설계하면 표가 늘고 경계가 흐려진다.

**원칙 넷.**

1. 학교를 경계로 삼는다. 모든 표에 학교 식별자를 두고 행 수준 보안으로 학교 밖 접근을 막는다.
2. **시간표 본문은 서버에 두지 않는다.** 나이스에서 언제든 다시 받을 수 있는 자료를
   굳이 복제할 이유가 없다. 서버가 들 것은 나이스가 주지 않는 것뿐이다.
3. 서버가 죽어도 도구는 돌아간다. 지금의 브라우저 저장이 기본이고 서버는 덧붙는 층이다.
4. 서버가 생기는 순간 화면의 보관 안내 문구를 사실에 맞게 고친다.

**표는 넷이면 된다.**

| 표 | 담는 것 | 왜 서버여야 하는가 |
|---|---|---|
| `school` | 시도교육청 코드, 학교 코드, 학교 이름 | 나이스 코드를 그대로 열쇠로 쓴다 |
| `member` | 학교 식별자, 이용자 식별자, 권한 | 누가 그 학교 사람인지 가른다 |
| `teacher_map` | 학교, 학년도, 학기, 학급, 과목, 교사 이름 | **나이스가 주지 않는 유일한 값이다** |
| `change_log` | 학교, 날짜, 변경 내용, 협조한 교사 | 협조 부담을 학교 단위로 세려면 필요하다 |

`teacher_map` 이 이 설계의 전부라고 봐도 된다. 나이스 공개 자료에는 교사가 없어
사람이 한 번은 채워야 하는데, 지금은 그 수고가 채운 사람의 브라우저에서 끝난다.
한 학교에 서른 명이 각자 채우는 것과 한 사람이 채워 서른 명이 쓰는 것의 차이가
이 제품에서 서버를 둘 유일한 값어치다.

시간표 본문(`assignments`)을 두지 않는 것이 핵심이다.
학교 코드와 학년도, 학기만 있으면 나이스에서 다시 그리고, 여기에 `teacher_map` 을 얹으면
같은 입력이 나온다. 그래서 서버가 드는 개인정보는 **교사 이름과 담당 과목**까지다.
연락처도, 학생 정보도, 근무 기록도 두지 않는다.

**인증.** 학교 메일 도메인 확인으로 시작하되 그것만으로 학교를 특정하지 못한다.
같은 시도교육청 도메인을 쓰는 학교가 수백 개이기 때문이다. 그래서 두 단계로 나눈다.
한 학교의 첫 이용자가 학교를 등록하며 관리자가 되고, 이후는 그 사람이 초대한다.
현행 제품의 회원 등록부가 하는 일과 같고, 상시 가동 PC 만 없다.

**행 수준 보안.** 모든 표에 `school_id` 를 두고 정책은 한 문장이다.
`school_id` 가 요청자의 `member` 행에 있는 학교와 같을 때만 읽고 쓴다.
예외를 하나라도 만들면 그 순간부터 학교 경계가 보증이 아니라 관행이 된다.

---

## 5. 저장이 막힌 브라우저

시간표는 이용자 브라우저에만 둔다. 그러면 브라우저가 저장을 거부할 때 갈 곳이 없다.
사생활 보호 모드이거나 저장 공간이 가득 찬 경우다.

이 실패를 조용히 삼키고 있었다. 화면은 "이 기기에 저장합니다"라고 말하는데 실제로는
저장되지 않았고, 선생님은 새로고침 한 번에 오늘 작업을 통째로 잃는다.
그 사실을 잃고 나서 알게 되는 것이 가장 나쁘다.

그래서 저장 함수가 성공 여부를 돌려주고, 실패하면 화면에 계속 남는 알림을 띄운다.
파일로 저장을 권하는 것까지가 그 알림의 몫이다.
브라우저 점검에 `localStorage.setItem` 이 던지는 판을 만들어 알림이 뜨는지 확인하는
단계를 넣어 두었다.

---

## 6. 고장 났을 때

| 증상 | 먼저 볼 곳 |
|---|---|
| 배포가 마지막에 실패 | 출력 디렉터리 설정. 정적 내보내기는 `out` 에 나온다 |
| 화면은 뜨는데 기능이 죽음 | 브라우저 콘솔의 CSP 차단 여부. `serve-checked.mjs` 로 재현 |
| 나이스 불러오기 실패 | 인증키 없이 5행 제한인지, 학교가 그 기간 자료를 올렸는지 |
| CI 만 실패 | `npm ci` 로 지역에서 재현. 잠금 파일 어긋남이 흔한 원인 |
| 추천이 이상함 | `npm run demo` 로 엔진만 떼어 확인 |
| 새로고침하면 작업이 사라짐 | 저장 실패 알림이 떴는지. 사생활 보호 모드가 흔한 원인 |

지역에서 배포와 같은 조건을 재현하는 명령이다.

```bash
npm ci
npm run build
node scripts/serve-checked.mjs   # 배포와 같은 헤더로 띄운다
npm run smoke                    # 다른 창에서
```
