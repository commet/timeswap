# 한국어 작성 규칙

사용자에게 보이는 모든 한국어에 적용한다. 화면 문구, 보고서, 커밋 메시지, 채팅 답변,
PR 본문을 모두 포함한다. (fsk-ops 저장소의 규칙을 이어받았다.)

## 1. 문장 부호

### Em 대시(—) 사용 금지

본문 어디에도 쓰지 않는다. 문맥에 맞는 것으로 바꾼다.

| 원래 쓰임 | 바꿀 것 |
|---|---|
| 앞말의 부연 | 마침표로 문장 분리, 또는 괄호 |
| 삽입구 양쪽 감싸기 | 괄호 |
| 목록 항목의 머리표 | 없애거나 콜론 |
| 이유 덧붙이기 | 마침표 후 새 문장 |

### 중간점(·)으로 낱말 나열 금지

쉼표, "와/과", 세로선(|) 중 문맥에 맞는 것을 쓴다.

예외는 데이터 형식이다. 저장 값의 구분자로 쓰이는 중간점은 바꾸지 않는다. 바꾸려면
쓰는 쪽과 읽는 쪽을 함께 고쳐야 한다.

## 2. 영어 직역 표현 금지

한국어 문서에서 쓰지 않는 번역체다. 아래 형태가 보이면 우리말 어법으로 고쳐 쓴다.

| 직역체 | 바꿀 것 |
|---|---|
| ~는 ~에서 나온다 | ~가 ~의 원인이다, ~때문에 생긴다 |
| A가 아니라 B다 | B다, A는 아니다 (두 문장으로) |
| ~를 마주한다 | ~가 생긴다, ~를 겪는다 |
| ~를 가진다 | ~가 있다 |
| ~에 대해 이야기한다 | ~를 다룬다, ~를 설명한다 |
| ~하는 것을 가능하게 한다 | ~할 수 있다 |
| ~에 의해 ~된다 | ~가 ~한다 (능동으로) |
| 그것은 ~라는 것을 의미한다 | 즉 ~다 |

## 3. 문체

- 화면에 보이는 문구는 합쇼체를 쓴다. "저장했습니다", "확인하십시오".
- 해요체를 쓰지 않는다. "저장했어요", "확인해 보세요", "진행할까요?"는 모두 금지다.
- 제목과 라벨은 명사구로 끝낸다. "무엇을 먼저 정비하는지"가 아니라 "정비 우선순위".
- 구어 부사를 쓰지 않는다. 그냥, 되게, 진짜, 엄청, 딱히, 살짝.
- 시혜적 표현을 쓰지 않는다. "채워줍니다"가 아니라 "채웁니다".

## 4. 건드리지 않는 것

- **사람이 말한 인용문.** 인터뷰 발언을 문체 교정하면 근거가 아니라 각색이 된다.
- **데이터 구분자.** 저장 값이나 파싱 대상의 기호는 문장 부호가 아니다.
- **코드 주석.** 설계 근거를 적는 자리이며 사용자에게 보이지 않는다. 다만 새로 쓸
  때는 위 규칙을 따르는 편이 읽기 좋다.

## 5. 확인 방법

이 저장소에는 아직 자동 검사가 없다. 화면 문구가 생기는 시점에 fsk-ops의
`tools/fix_copy_style.py`(소스 검사)와 `tests/test_copy_punctuation.py`(렌더링
실측 검사)를 이식한다. fsk-ops에서 얻은 교훈: 소스 검사만으로는 `<script>` 안의
문구, JSON 데이터, HTML 엔티티(`&mdash;`), 빈 값 표시(`x || "—"`)를 놓친다.
잠금의 중심은 렌더링 검사다.
