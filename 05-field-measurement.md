# 05. 학교 현실을 재는 방법과 그 결과

짐작으로 만든 규칙이 실제 자료에서 어떻게 되는지 재어 본 기록이다.
다섯 번에 걸쳐 3곳, 24곳, 41곳, 217곳, 그리고 초중고와 특수학교 371곳을 쟀다.
그때마다 결론이 한 번씩 뒤집혔다.

| 회차 | 무엇이 뒤집혔나 |
|---|---|
| 1 → 2 | 표본 셋으로 정한 기준값 1.5가 24곳에서 1학년 중앙값이었다. 절반을 잘못 보고 있었다 |
| 2 → 3 | 특성화고에서는 결론이 맞아도 이유가 틀렸다. 과목이 많은 까닭이 선택과목이 아니다 |
| 3 → 통과 | 학교 하나를 통째로 통과시키니 하루 종일 실습을 휴업일로 읽고 있었다 |
| 3 → 4 | 인증키로 217곳을 재니 기준값 2.2가 아니라 **지표 자체**가 틀렸다 |
| 4 → 5 | 초중고를 다 넣으니 특수학교에서 세 학급이 하나로 합쳐지고 있었다 |

그 밖에 자리마다 이동수업인지 묻던 규칙은 판별력이 없었고 학년 단위 신호가 훨씬 강했다.

**4회차에서 앞선 세 회차의 숫자가 대부분 갈렸다.** 아래에서 회차별로 그대로 남겨 두었다.
지운 것이 아니라 무엇이 왜 틀렸는지가 이 기록의 알맹이기 때문이다.
지금 코드가 쓰는 값을 찾는다면 8절과 9절로 바로 가면 된다.

---

## 1. 어떻게 쟀는가

> **1절부터 7절까지는 인증키 없이 잰 것이다.** 그 방식으로는 한 학급 하루에 5행만
> 오고, 그 5행은 늘 1교시부터 잘린다. 아래 숫자에는 6교시와 7교시가 없다.
> 무엇이 그래서 틀렸는지는 8절에 있다.

### 인증키 없이 학교 시간표를 통째로 받는 법

3장에 적었듯 인증키가 없으면 한 번에 5행만 온다. 쪽 넘김도 듣지 않는다.
그런데 **질의를 좁히면 그 5행 안에 필요한 것이 들어온다.**

```
ALL_TI_YMD + GRADE + CLASS_NM 으로 좁히면 그 학급 그날이 5행 안에 들어온다
```

학급마다 날짜마다 한 번씩 부르면 학교 하나의 주간 시간표가 재구성된다.
열두 갈래로 나눠 부르니 학교 하나에 30초쯤 걸렸다.

### 부딪힌 벽

**`PERIO` 필터는 동작하지 않는다.** 6교시를 물어도 1교시 행이 온다.
그래서 5행 제한을 뚫을 방법이 없고, **인증키 없이는 학급당 하루 5교시까지만 보인다.**
아래 숫자는 모두 그 범위의 것이다. 6, 7교시가 빠져 있다.

### 유형을 골라 뽑는 법

`HS_SC_NM`(계열) 로 거를 수 없다. 걸어도 무시하고 일반고부터 다섯 줄을 준다.
`pIndex` 도 듣지 않아 뒷장으로 넘어갈 수 없다.

대신 **`SCHUL_NM` 이 부분 일치로 전국을 훑는다.** 교육청과 이름 조각으로 좁혀
다섯 줄씩 받아 모으면 유형별로 쌓인다. 교육청 17곳에 이름 조각 26개, 442번 불러 394곳을 모았다.

유형은 받아 온 `HS_SC_NM` 을 그대로 읽는다. **이름으로 유형을 짐작하지 않는다.**
공업고 가운데 특성화고도 있고 마이스터고(자료에서는 특목고)도 있다.

### 표본

2026학년도 1학기다. 늘려 가며 세 번 쟀다.

| 회차 | 표본 | 무엇을 쟀나 |
|---|---|---|
| 1 | 3곳 | 학년별 과목 다양성 |
| 2 | 24곳 | 같은 값, 기준값 재조정 |
| 3 | 일반고 12, 특성화고 17, 특목고 12 | 유형별 차이, 과목명 표시, 덩어리 편성 |

학교 자료는 저장소에 남기지 않았다. 통계만 뽑고 원본은 버렸다.

---

## 2. 고교학점제가 자료에 보인다

학년이 올라갈수록 한 학년에 동시에 열리는 과목 종수가 뚜렷하게 는다.
**처음 세 학교로 보고 내린 결론을 24곳으로 넓혀 다시 쟀고, 결론이 바뀌었다.**

### 처음 세 학교 (일반고 둘, 특목고 하나)

| 학교 | 1학년 | 2학년 | 3학년 |
|---|---|---|---|
| 일반고 A | 13종 / 13학급 = **1.0** | 1.4 | **2.0** |
| 일반고 B | 14 / 13 = **1.1** | 1.8 | **2.1** |
| 특목고 | 17 / 10 = **1.7** | 2.1 | **2.5** |

여기서 "공통과목 학년은 1.0 언저리"라고 읽고 기준값을 1.5로 잡았다. **이것이 틀렸다.**

### 24곳으로 넓힌 결과

| 학년 | 최소 | 1사분위 | 중앙 | 3사분위 | 최대 |
|---|---|---|---|---|---|
| 1학년 (n=24) | 1.1 | 1.3 | **1.5** | 1.9 | 3.3 |
| 2학년 (n=24) | 0.8 | 1.6 | 2.2 | 2.9 | 3.7 |
| 3학년 (n=21) | 1.0 | 2.4 | **2.7** | 3.3 | 6.9 |

**1학년 중앙값이 정확히 1.5다.** 기준을 1.5로 두면 1학년의 절반을 선택과목 구간으로
잘못 본다. 표본 셋으로 정한 값이 하필 넓힌 표본의 한가운데였다.

학년이 오를수록 는다는 방향은 그대로다. 다만 겹치는 구간이 넓다.
3학년 최대는 6.9(학급 9개에 과목 62종)까지 간다.

### 기준값을 다시 골랐다

| 기준 | 1학년 오탐 | 3학년 미탐 |
|---|---|---|
| 1.5 | **54%** | 10% |
| 2.0 | 17% | 10% |
| **2.2** | **4%** | **14%** |
| 2.5 | 4% | 43% |

**2.2를 골랐다.** 놓치는 쪽보다 잘못 알리는 쪽이 나쁘기 때문이다.
공통과목만 도는 1학년에서 "선택과목이라 교체가 어렵다"고 하면 그 말이 틀렸고,
한 번 틀린 안내는 다음 안내까지 못 믿게 만든다.

### 알아 둘 혼동 요인

학급이 적은 학교는 비율이 높게 나온다. 학급 7개 이하는 평균 2.27, 10개 이상은 1.83이었다.
**작은 학교에서 오탐이 조금 더 난다.** 비율이 학급 수로 나눈 값이라 생기는 성질이다.

---

## 3. 우리 규칙을 재어 보니 판별력이 없었다

이동수업 후보를 알리는 규칙을 셋 놓고 같은 자료로 쟀다. 사흘 × 5교시 기준 건수다.

| 학교 | 학년 | 과목/학급 | 규칙 A | 규칙 B (당시 규칙) | 규칙 C |
|---|---|---|---|---|---|
| 일반고 A | 1 | 1.0 | 43 | 5 | 38 |
| 일반고 A | 2 | 1.4 | 35 | 5 | 30 |
| 일반고 A | 3 | 2.0 | 31 | 6 | 26 |
| 일반고 B | 1 | 1.1 | 34 | 5 | 29 |
| 일반고 B | 3 | 2.1 | 21 | 5 | 16 |
| 특목고 | 3 | 2.5 | 18 | 5 | 13 |

- **규칙 A**: 같은 교시에 같은 과목을 2개 반 이상 듣는다
- **규칙 B**: 거기에 더해 그 과목이 걸린 모든 교시에서 학급 구성이 똑같아야 한다
- **규칙 C**: 거기에 더해 그 학년 전체 학급은 아니어야 한다

**규칙 B가 모든 학년에서 5에서 6으로 거의 일정하다.** 이것이 문제다.
공통과목만 도는 1학년과 선택과목이 가득한 3학년에서 같은 빈도로 뜬다면,
그 규칙은 이동수업을 가려내는 것이 아니라 다른 무언가를 세고 있는 것이다.

합성 학교에서는 오탐률 1.0퍼센트로 잘 나왔다. 합성 학교가 현실과 달랐기 때문이다.
**작은 자료로 만든 규칙을 큰 자료로 재지 않으면 이런 일이 생긴다.**

---

## 4. 그래서 무엇을 바꿨는가

묻는 단위를 **자리에서 학년으로** 옮겼다.

과목 종수를 학급 수로 나눈 값이 2.2를 넘으면 그 학년을 과목이 많은 학년으로 본다.
그 학년의 수업을 고르시면 배경을 먼저 알린다.

> **이 지표는 8절에서 통째로 갈렸다.** 지금 코드는 공통도를 쓴다. 아래는 3회차 기록이다.

> 3학년은 학급 13개에 과목 26종이 열립니다. **선택과목 구간**이라 수강 학생이 여러
> 학급에 흩어져 있고, 그런 수업은 통째로 옮길 자리를 찾기 어렵습니다.
> 교체가 안 되면 아래 보강을 보십시오.

자리마다 뜨는 알림이 아니라 배경 설명이다. 고3에서는 수업 열에 서넛이 이동수업인데
그걸 자리마다 알리면 알림이 아니라 소음이 된다.

기존의 자리별 알림은 그대로 두었다. 판별력은 약하지만 실제로 묶을 자리를 찾았을 때
한 번에 묶는 통로로는 쓸모가 있다.

---

## 5. 유형을 나눠 재니 같은 문장이 틀린 곳이 있었다

특성화고와 특목고를 각각 열 곳 넘게 모아 다시 쟀다. 세 가지가 나왔다.

### 5.1 과목명 앞의 별표는 전문교과 실습이다

| 유형 | 학교 | 별표가 붙은 칸의 몫 |
|---|---|---|
| 일반고 | 12곳 | 0% |
| 특성화고 | 10곳 | 14~34% |

`* 도면해독(선반가공)` 처럼 온다. 보강과 겹치면 `[보강]* 실내건축설계 기획` 이다.
같은 과목이 표시가 붙은 채와 안 붙은 채로 함께 나온 적은 한 번도 없었다. 규칙적이다.

이걸 과목 이름의 일부로 두면 화면에 별표가 그대로 나오고, 과목명을 열쇠로 쓰는
교사 배정표에서 같은 과목이 표시 유무로 갈릴 수 있다.

### 5.2 과목이 많은 까닭이 학교마다 다르다

**특성화고 13곳 38개 학년 가운데 29개가 기준값 2.2를 넘는다.**
그런데 그 학교에서 과목이 많은 까닭은 고교학점제 선택과목이 아니라 학과별 전공 편성이다.
학생은 학과 안에서 학급 그대로 함께 다닌다.

같은 문장을 쓰면 결론(교체가 어렵다)은 맞는데 이유가 틀린다.
그리고 이 기록 아래에 이미 적어 두었듯, 한 번 틀린 안내는 다음 안내까지 못 믿게 만든다.

전문교과 몫으로 갈랐다. 기준은 학교 25곳을 재어 정했다.

| 무리 | 학교 | 최소 | 중앙 | 최대 |
|---|---|---|---|---|
| 과학고, 외국어고, 예술고, 체육고 | 9 | 0.00 | 0.00 | 0.00 |
| 특성화고와 마이스터고 | 16 | 0.31 | 0.54 | 0.67 |

**0.00 과 0.31 사이에 놓인 학교가 하나도 없다.** 그 사이 어디에 두어도 판정이 같아
가운데인 0.15로 둔다.

학년이 아니라 학교 전체로 재는 이유가 따로 있다.

| 학년 | 표본 | 최소 | 중앙 | 최대 |
|---|---|---|---|---|
| 특성화고 1학년 | 13 | 0.00 | **0.00** | 0.20 |
| 특성화고 2학년 | 13 | 0.20 | 0.47 | 0.78 |
| 특성화고 3학년 | 12 | 0.46 | 0.73 | 0.86 |

특성화고라도 1학년에는 전문교과가 거의 없다. **학년만 보고 판정하면 특성화고 1학년을
일반고로 읽는다.** 학교 단위로 재야 두 무리가 겹치지 않는다.

### 5.3 실습은 여러 교시를 이어 붙여 편성한다

학급 하루 안에서 같은 과목이 이어진 길이를 셌다.

| 무리 | 학교 | 덩어리 안 수업 칸 | 길이별 덩어리 |
|---|---|---|---|
| 일반고 | 10곳 | 3.5% | 길이 2만 105개, 길이 3 이상 **0개** |
| 마이스터고와 예술고 | 4곳 | 23~58% | 길이 2가 287개, 길이 3이 108개, 길이 4가 57개 |

**일반고에서는 길이 3 이상이 하나도 나오지 않았다.** 거기 보이는 길이 2는 이어 하려고
붙인 것이 아니라 편성하다 붙은 것일 때가 많다. 직업계는 절반 가까이가 덩어리 안에 있다.

> **이 문장은 8절에서 뒤집혔다.** 인증키로 6, 7교시까지 보니 일반고와 자율고에도
> 길이 3이 188개, 길이 4 이상이 78개 있었다. 4교시에서 6교시로 걸친 덩어리가
> 5교시까지만 보이는 자료에서는 길이 2로 보였을 뿐이다.

그 가운데 한 교시만 다른 날로 보내는 안은 표 위에서만 성립한다.
앞 시간에 준비하고 뒤 시간에 만드는 수업이기 때문이다.

### 5.4 한 칸에 과목이 둘 오는 학교가 있다

| 학교 | 1~3교시 칸 가운데 과목이 둘인 비율 |
|---|---|
| 국립전통예술고 | 15.4% |
| 울산예술고 | 5.4% |
| 나머지 35곳 | 0.0% |

예술 계열에서 전공 실기를 나눠 편성한 자리다.

> **이 절은 8절에서 통째로 뒤집혔다.** 인증키로 217곳을 재니 일반고와 자율고 109곳
> 가운데 56곳에, 특성화고와 마이스터고 79곳 가운데 52곳에 복수 과목 칸이 있었다.
> 특성화고는 칸의 15.81%다. "예술 계열 말고는 0%"가 아니었다.
>
> 원인은 표본이 아니라 **수집 방식**이었다. 인증키 없이 학급 하루를 5행으로 받는데
> 한 칸에 과목이 둘이면 그 칸이 행을 둘 먹는다. 그래서 뒤 교시가 통째로 잘리고
> 복수 과목 칸이 체계적으로 덜 보였다. 그리고 이 절은 애초에 "1~3교시 칸 가운데"로
> 범위를 좁혀 놓았다. 좁힌 이유 자체가 그 잘림이었다.
> **재려던 것을 볼 수 없는 방식으로 재고 있었다.**

---

## 6. 통계를 넘어 실제 학교 하나를 통째로 통과시켜 보았다

분포를 재는 것과 자료를 통과시키는 것은 다른 일이다.
특성화고 한 곳(부천공업고, 열흘치)을 받아 어댑터부터 추천까지 전 과정을 돌렸다.
**합성 자료에서는 드러나지 않던 잘못이 넷 나왔다.**

### 6.1 하루 종일 실습을 휴업일로 읽었다

"그 학급 그날의 관측 칸이 모두 같은 값이면 휴업"이라는 규칙이 어긋났다.
전공 실습을 하루 종일 편성하는 학급이 흔하다.

```
휴업으로 잡힌 (학급, 날짜) 짝  41개
그 값들  도면해독(선반가공), 퍼티작업, 공업 화학, 환경 기술, 건축 일반 ...
관측 칸의 18%가 수업이 아닌 것으로 버려짐
받아 온 열흘이 통째로 학교 휴업일 목록에 오름
```

**41건이 모두 한 학급짜리였다.** 휴업일과 학년 행사는 학년이나 학교 전체를 덮는다.
그래서 몇 학급에 걸쳐 있는지를 함께 본다. 전문교과 표시가 붙은 값도 뺀다.

비율이 아니라 "관측된 학급 전부"로 본다. 한 학급만 받아 온 자료도 다뤄야 하기 때문이다.
그때는 1/1 이라 통과하고, 학교 전체를 받아 오면 한 학급짜리 실습이 걸러진다.
**자료가 두꺼울수록 정확해지고 얇아도 쓸 수 있는 규칙이다.**

고친 뒤 수업 칸이 940개에서 1145개로 늘고 휴업일 목록이 비었다.

### 6.2 교사 표를 다 채우지 않으면 성립하는 안이 하나도 없었다

나이스 학급 시간표에는 교사가 없다. (학급, 과목)을 교사로 잇는 표는 사람이 채우는데
학교 하나에 그 짝이 수백 개다. **처음부터 다 채우고 시작하는 사람은 없다.**

같은 학교 자료로 표를 60%만 채우고 재었다.

```
추천안 459개, 그중 학급이 실제로는 수업 중인 교시로 밀어 넣는 이동 282건
```

덜 채운 칸을 배정에서 빼니 그 자리가 빈 시간으로 보였다.
**아는 만큼만 채우고 쓰기 시작하는 것이 정상인데, 그 상태에서 나오는 안이 다 틀렸다.**

담당을 몰라도 "그 시간은 차 있다"는 사실만 따로 들고 가게 고쳤다.
그 자리로는 옮기지 않고 빈 시간 계산에도 수업으로 센다. 다만 교사를 모르므로
그 수업 자체를 옮기거나 맞바꾸는 대상으로는 삼지 않는다.

고친 뒤 같은 자료에서 틀린 이동이 0건이 되었고 쓸 수 있는 추천안은 387개가 남았다.

### 6.3 자료가 없는 요일을 빈 시간으로 읽었다

같은 학교 자료를 폰 화면에 띄워 보니 수요일이 통째로 비어 있었다. 실제 학교가 수요일에
수업을 안 할 수는 없다. 수집할 때 그 요일을 안 받은 것이었다.

앱은 없는 자료를 정직하게 비워 두었는데, **그 빈 자리를 엔진이 옮겨도 되는 자리로 읽었다.**

```
추천안 488개 가운데 수요일로 옮기라는 안 21개
```

담당 교사를 안 채운 자리와 똑같은 잘못이다. 모르는 것을 없는 것으로 바꾸면 그 자리가
비어 보인다. 수업이 하나도 관측되지 않은 요일을 찾아 탐색에서 뺐고, 화면에 그 요일과
이유를 함께 적는다. 고친 뒤 같은 자료에서 0개가 되었다.

### 6.4 인증키 없이는 학교 하나를 받을 수 없다

"인증키 (선택)" 이라고 적어 두었던 것이 잘못이었다. 재어 보니 선택이 아니다.

```
GET hisTimetable  (학급 하나, 하루, 키 없음)
→ list_total_count: 6      그날 그 학급은 6교시다
→ 받은 행: 5               6교시는 오지 않는다
```

쪽 넘김도 듣지 않는다. 게다가 앱은 학교 전체를 한 번에 묻기 때문에, 키가 없으면
**5주치 수천 행 가운데 5행**을 받는다. 그것으로 만든 시간표는 거의 다 빈 시간이고,
빈 시간은 옮겨도 되는 자리로 읽힌다.

경고만 띄우고 통과시키던 것을 멈추게 바꿨다. 잘린 자료로는 다음 단계로 가지 않는다.
받은 뒤에는 학급 수와 요일과 교시를 화면에 적어 온전한지 사용자가 볼 수 있게 했다.

### 6.5 분반과 합반이 한 자리에서 만나면 시간표가 깨졌다

3학년 6반이 두 강좌로 나뉘는데, 그중 한 강좌를 맡는 분이 같은 교시에 3학년 5반도 맡고 있었다.
셋이 한 덩어리인데 분반 묶기와 합반 묶기를 따로 돌려 두 조각으로 갈렸다.
**학급 중복과 교사 중복 검사에 42건이 걸려 시간표 자체가 성립하지 않았다.**

이어지는 것을 끝까지 따라가 한 덩어리로 묶도록 바꿨다.
고친 뒤 불변식 위반이 0이 되었고, 추천안 1148개를 적용해도 모두 성립한다.

---

## 7. 그래서 또 무엇을 바꿨는가

1. **별표를 떼어 `pro` 표시로 들고 다닌다.** 화면과 배정표에서 과목명은 깨끗해지고,
   전문교과라는 사실은 따로 남는다.
2. **과목이 많은 까닭을 갈라 다른 문장을 낸다.** 전문교과 학교에는
   "학과별 전공 편성이라 과목이 많습니다"라고 말한다.
3. **전문교과 실습의 보강은 실습을 맡는 분을 먼저 부른다.** 같은 과목 가점보다 크게 잡아,
   과목이 달라도 실습을 맡는 분이 일반교과만 맡는 분보다 앞에 온다. 빼지는 않는다.
4. **덩어리를 쪼개는 안을 뒤로 민다.** 길이 2는 -8, 길이 3은 -16, 길이 4는 -24다.
   같은 날 덩어리 끝에 다시 붙는 이동은 길이가 그대로라 깎지 않는다.
5. **한 칸의 두 과목을 둘 다 남기고 한 묶음으로 묶는다.** 한쪽만 옮기는 안이 나오지 않는다.
6. **담당을 아직 안 채운 자리를 차 있는 자리로 다룬다.** 아는 만큼만 채우고 써도
   틀린 안이 나오지 않는다. 화면에는 그 자리가 몇 개인지와 그만큼 찾을 수 있는 교체가
   줄어든다는 사실을 함께 적는다. 파일로 저장할 때도 함께 담는다.

---

## 8. 인증키를 받아 전국 217곳을 다시 쟀다

앞선 일곱 절은 인증키 없이 잰 것이다. 그 방식은 한 학급 하루에 5행만 주고 그 5행은
늘 1교시부터 잘린다. 6, 7교시가 통째로 빠져 있었다.

인증키를 받아 다시 쟀다. 세 가지가 달라졌고, 그 가운데 가장 큰 것은 예상 밖이었다.

### 8.1 어떻게 쟀는가

키가 있으면 `pSize` 와 `pIndex` 가 듣는다. 학교 하나를 한 번에 받는다.

```
GET https://open.neis.go.kr/hub/hisTimetable
    ?KEY={인증키}&Type=json&pIndex=1&pSize=1000
    &ATPT_OFCDC_SC_CODE={시도코드}&SD_SCHUL_CODE={학교코드}
    &TI_FROM_YMD=20260608&TI_TO_YMD=20260612
```

키 없이 같은 호출을 하면 이렇게 온다.

```
알린 총 행 수: 739
실제 받은 행 수: 5
받은 교시들: [1, 2, 3, 4, 5]
```

키를 넣으면 739행 전부가 오고 교시는 1부터 7까지다. 8교시 이상도 드물게 있다.

`SCHUL_KND_SC_NM`(학교급) 필터도 키가 있어야 듣는다. 3절에 "계열로 거를 수 없다"고
적었던 것도 키 없을 때의 증상이었다. 교육청 17곳에서 고등학교 2,407곳을 받아
계열별로 나눈 뒤 무작위로 240곳을 뽑았고, 그 가운데 217곳이 시간표를 공개하고 있었다.

| 계열 | 표본 | 근거 필드 |
|---|---|---|
| 일반고 | 82 | `HS_SC_NM` |
| 자율고 | 27 | 〃 |
| 특목고 | 44 | 〃 (마이스터고 12곳 포함) |
| 특성화고 | 64 | 〃 |

**계열명으로 전문계인지 가르면 안 된다.** 특성화고로 등록된 대안학교가 전문교과를
하나도 안 쓴다. `HS_GNRL_BUSNS_SC_NM`(일반계 또는 전문계)이 정답 자리다.

### 8.2 6, 7교시는 예상만큼 중요하지 않았다

가장 먼저 확인한 것이 이것이다. 같은 217곳 자료를 1~5교시로 잘라서 재고 1~7교시로
다시 재어 나란히 놓았다.

| 학년 | 1~5교시만 (중앙) | 1~7교시 (중앙) |
|---|---|---|
| 1학년 | 2.00 | 2.00 |
| 2학년 | 3.12 | 3.15 |
| 3학년 | 4.33 | 4.39 |

거의 안 움직인다. 뒤에 쓸 새 지표로도 재어 보니 오탐과 미탐이 1퍼센트포인트 안에서만
달라졌다. **잘린 교시가 기준값을 망친 것이 아니었다.**

### 8.3 망친 것은 24곳이라는 표본이었다

기준값 2.2는 24곳에서 "1학년 오탐 4%"라고 재고 고른 값이다. 217곳에 적용하면 이렇다.

| 무리 | 1학년 오탐 | 3학년 미탐 |
|---|---|---|
| 일반고와 자율고 | **17%** | 3% |
| 특목고 | **50%** | 11% |

특목고 1학년 절반에게 "선택과목이라 교체가 어렵다"는 틀린 안내가 나간다.
2.2를 고른 이유가 바로 그 오탐을 막는 것이었는데 정반대가 되었다.

기준값을 옮겨도 낫지 않았다. 4.0까지 올려도 오탐이 9%와 25%로 남는데 3학년 미탐이
64%로 갔다. **값이 아니라 지표가 문제였다.**

### 8.4 지표가 학급 수에 끌려다니고 있었다

과목 종수를 학급 수로 나눈 값이다. 분모가 학급 수라 학년이 작으면 무조건 커진다.

| 학년 학급 수 | 표본 | 과목 종수 / 학급 수 (중앙) |
|---|---|---|
| 1~2개 | 91 | **10.00** |
| 3~4개 | 95 | 5.33 |
| 5~7개 | 139 | 3.00 |
| 8~10개 | 218 | 2.32 |
| 11개 이상 | 105 | **1.91** |

같은 교육과정을 돌려도 다섯 배가 벌어진다. 24곳 표본은 학급 9~13개짜리 일반고에
쏠려 있어서 이 벌어짐이 안 보였다. 학급 1~2개인 학년이 91개나 되는 것을 그때는 몰랐다.

### 8.5 재는 것을 바꿨다

화면에 나가는 문장은 "교체보다 보강이 현실적일 수 있습니다"이다.
그렇다면 재야 할 것은 과목이 몇 종인지가 아니라 **교체 상대가 있는지**다.

**공통도**로 바꿨다. 과목마다 그 과목을 듣는 학급이 학년의 몇 할인지 구해 평균한다.
학급 수로 먼저 나눈 뒤 평균하므로 학년 규모에 덜 끌려다닌다.

| 무리 | 1학년 중앙 | 2학년 중앙 | 3학년 중앙 |
|---|---|---|---|
| 일반고와 자율고 (106곳) | 0.91 | 0.50 | 0.39 |
| 특목고 (28곳) | 0.75 | 0.56 | 0.57 |

학급 수 편향도 5.2배에서 2.2배로 줄었다. 남은 벌어짐은 치우침이 아니다.
학급이 하나뿐인 학년에는 함께 움직일 다른 학급이 정말로 없다.

| 기준 | 일반고 1학년 오탐 | 일반고 3학년 미탐 | 특목고 1학년 오탐 | 특목고 3학년 미탐 |
|---|---|---|---|---|
| 0.60 | 3% | 9% | 18% | 36% |
| **0.65** | **3%** | **7%** | **25%** | **25%** |
| 0.70 | 4% | 5% | 29% | 14% |

**0.65를 골랐다.** 표본을 절반으로 갈라 스무 번 다시 고르게 하니 0.59에서 0.70 사이,
중앙 0.67이 나왔다. 0.65는 그 안에 넉넉히 든다.

특목고 오탐 25%는 그대로 두었다. 과학고와 외국어고 1학년에는 실제로 계열별 강좌가
열려 오탐이라 센 것 가운데 오탐이 아닌 것이 섞여 있다. 자료로 가릴 수 없어 숫자를
그대로 적어 둔다. **없는 정확도를 적어 두면 다음 사람이 속는다.**

### 8.6 전문교과 몫 0.15는 값이 살고 근거가 죽었다

`HS_GNRL_BUSNS_SC_NM` 을 정답으로 놓고 훑었다.

| 기준 | 전문계 미탐 | 일반계 오탐 | 오분류 합 |
|---|---|---|---|
| 0.05 | 2.5% | 2.9% | 6 |
| **0.15** | **3.8%** | **2.2%** | **6** |
| 0.20 | 7.6% | 1.4% | 8 |
| 0.30 | 13.9% | 0.7% | 12 |

217곳에 오분류 6곳으로 최저다. 값은 그대로 두었다.

**그런데 그때 붙인 근거는 틀렸다.** 25곳을 보고 "0.00과 0.31 사이에 학교가 하나도
없으니 값이 흔들려도 결과가 안 바뀐다"고 적었다. 217곳에서는 그 구간에 13곳이
들어앉는다. 0.081, 0.141, 0.167, 0.169, 0.175, 0.180, 0.220, 0.250, 0.260, 0.279,
0.286, 0.293, 0.305이다. 기준 바로 옆에 여섯 곳이 서 있다.

**맞는 값에 틀린 근거가 붙어 있으면 다음 사람이 그 근거를 믿고 값을 옮긴다.**

### 8.7 217곳을 엔진에 통째로 통과시켰더니 학년 판정이 죽어 있었다

분포를 재는 것과 자료를 통과시키는 것은 다른 일이다. 217곳 전부를 어댑터부터
추천까지 넣었다. 첫 결과에서 **`gradeShapes` 가 217곳 전부에서 빈 배열을 돌려주었다.**

학년을 학급 키에서 정규식으로 캐내고 있었다. 그런데 나이스 자료의 학급 키는 이렇다.

```
["7010084","2026","주간","","","1","1"]
```

첫 숫자가 학교 코드다. 그것을 학년으로 읽으니 7010084가 나오고, 1에서 12 사이가
아니라 버려졌다. **학년 안내는 실제 나이스 자료에서 한 번도 뜬 적이 없었다.**

합성 자료 시험이 이것을 못 잡은 이유가 분명하다. 시험은 학급 이름을 `1-3` 으로 쓴다.
거기서는 첫 숫자가 정말 학년이다. **시험이 현실과 다른 모양의 자료만 보고 있었다.**

학년을 `klassGrade` 로 명시해 넘기도록 고쳤고, 나이스 행에서 출발하는 회귀 시험을
붙였다. 고친 것을 되돌려 그 시험이 실제로 실패하는지 확인했다.

### 8.8 고친 뒤 217곳을 다시 통과시킨 결과

교사 표는 학교가 채웠을 법하게 만들었다. (학년, 과목)마다 교사를 여러 분 두고 같은
교시에 겹치지 않게 나눠 맡기되, 학급 무리가 그 과목의 모든 교시에서 똑같으면
한 사람이 모아 가르치는 이동수업으로 보아 교사를 하나만 두었다.

| 재어 본 것 | 결과 |
|---|---|
| 예외로 멈춘 학교 | 0 / 217 |
| 시간표가 성립하지 않은 학교 | 0 / 217 |
| 학년 판정이 빈 학교 | 0 / 217 |
| 추천안을 실제로 적용했을 때 시간표가 깨진 경우 | 0 / 3,460회 |
| 추천안을 적용해 빈 시간이 늘어난 경우 | 0 / 3,460회 |
| 후보가 하나도 없던 요청 | 114 / 3,460 (3.3%) |
| 묶음이 생긴 학교 | 176 / 217 (묶음 2,149개) |
| 학교당 소요 | 중앙 370ms, 최대 2,030ms |

추천안을 만들어 놓고 끝내지 않고 **실제로 적용해 다시 검사했다.** 표 위에서만 맞는
안이 나오는지가 이 도구에서 가장 무서운 실패라서다. 3,460회에 0건이다.

후보가 없는 3.3%는 결함이 아니다. 옮길 자리가 정말 없는 수업이 있다.

고친 뒤 학년 판정은 이렇게 나온다.

| 무리 | 1학년 | 2학년 | 3학년 |
|---|---|---|---|
| 일반계 | 보통 96% | 선택과목 54% | 선택과목 65% |
| 전문계 | 전공실습 27% | 전공실습 71% | 전공실습 85% |

일반계 1학년 오탐 4%가 8.5절에서 잰 값과 맞는다.

이 검사는 `packages/engine/test/field-run.test.ts` 에 남겼다. 학교 자료는 저장소에
두지 않으므로 환경 변수로 파일을 가리켜야 돌고, 없으면 통과가 아니라 **건너뜀**으로
표시된다. 아무것도 안 재고 초록으로 보이는 것이 가장 나쁘다.

### 8.9 덩어리 편성과 복수 과목 칸

여기서는 6, 7교시가 정면으로 작용했다.

| 무리 | 덩어리 안 수업 (1~5교시) | 덩어리 안 수업 (1~7교시) | 길이 3 | 길이 4 이상 |
|---|---|---|---|---|
| 일반고와 자율고 | 4.4% | **9.8%** | 188 | 78 |
| 특목고 | 13.9% | 19.8% | 133 | 29 |
| 특성화고와 마이스터고 | 35.3% | 41.0% | 1,366 | 427 |

5.3절의 "일반고에서는 길이 3 이상이 하나도 나오지 않았다"가 여기서 뒤집힌다.
4교시에서 6교시로 걸친 덩어리가 5교시까지만 보이는 자료에서는 길이 2로 보였다.

| 무리 | 복수 과목 칸 | 그런 칸이 있는 학교 |
|---|---|---|
| 일반고와 자율고 | 1.98% | 56 / 109 |
| 특목고 | 4.18% | 17 / 27 |
| 특성화고와 마이스터고 | 15.81% | 52 / 79 |

5.4절의 "예술 계열 말고는 0%"도 뒤집힌다. 이쪽은 원인이 교시가 아니라 수집 방식이다.
1~3교시로 잘라서 다시 재도 일반고가 2.14%로 나온다. 인증키 없이 학급 하루를 5행으로
받으면 한 칸에 과목이 둘인 자리가 행을 둘 먹어 뒤 교시가 통째로 잘린다.
**재려던 것을 볼 수 없는 방식으로 재고 있었다.**

엔진이 이 칸을 다루는 방식 자체는 그대로 두었다. 한 칸에 강좌가 둘이면 담당을 둘 다
알 때만 배정으로 만들고, 하나라도 모르면 그 칸은 "차 있다"는 사실만 남긴다.
틀린 것은 코드가 아니라 "드물다"고 적은 문서였다.

### 8.10 표를 덜 채우면 새 지표도 무너진다

지표를 바꾸고 나서 한 가지를 더 재었다. **교사 표를 다 채운 상태만 재고 끝내면 안 된다.**
사람은 아는 만큼 채우고 쓰기 시작하기 때문이다. 6.2절에서 같은 함정에 한 번 빠졌다.

217곳 자료로 교사 표를 무작위로 덜 채워 가며 판정이 어떻게 흔들리는지 재었다.

| 채운 비율 | 판정 뒤집힘 | 그중 오탐 |
|---|---|---|
| 100% | 0.0% | 0.0% |
| 90% | 5.4% | 5.4% |
| 80% | 10.6% | 10.6% |
| 70% | 22.1% | 21.9% |
| 60% | 32.6% | 32.4% |

**뒤집힘이 거의 전부 오탐이다.** 까닭이 분명하다. 한 과목을 학급 넷이 다 듣는데
둘만 채워 두면 그 과목은 0.5 로 계산된다. 덜 채운 자리가 "그 학급은 그 과목을
안 듣는다"로 읽힌다. **모르는 것이 없는 것으로 바뀐다.** 이 저장소가 같은 방식으로
세 번 물린 자리다.

담당을 모르는 몫으로 억제하기로 했다. 억제 기준별로 남는 것이다.

| 억제 기준 | 억제된 몫 | 남은 오탐 | 제대로 뜬 안내 |
|---|---|---|---|
| 없음 | 0.0% | 14.6% | 56.5% |
| 0.35 | 24.8% | 6.2% | 42.2% |
| **0.25** | **37.3%** | **3.2%** | **35.2%** |
| 0.15 | 55.8% | 1.3% | 24.7% |

**0.25 를 골랐다.** 지표 자체의 오탐이 3%다. 지키려는 것보다 헐거운 보호 장치는
약한 고리가 된다. 더 조이면 제대로 뜰 안내까지 절반이 사라진다.

217곳을 교사 표 70%만 채운 상태로 다시 통과시켜 확인했다.

| | 학년 642개 기준 |
|---|---|
| 억제 장치가 없었다면 나갔을 오탐 | 198개 (30.8%) |
| 억제 장치를 켠 뒤 남은 오탐 | 5개 (**0.8%**) |
| 억제된 판정 | 500개 (77.9%) |

70%만 채운 상태에서 78%를 억제하는 것은 과하지 않다. 그 상태에서는 정말 가릴 수
없기 때문이다. 표를 채울수록 안내가 돌아온다. 자료를 주면 더 쓸모 있어지는 쪽이 맞다.

---

## 9. 초등학교와 중학교, 특수학교를 넣어 보았다

8절까지는 고등학교만 재었다. 전국 학교는 12,584곳이고 그 가운데 고등학교는 2,407곳이다.
나머지를 넣어 보니 고등학교에만 맞춰 둔 곳이 세 군데 드러났다.

| 학교급 | 전국 | 이번 표본 | 자료 있음 |
|---|---|---|---|
| 초등학교 | 6,341 | 70 | 57 |
| 중학교 | 3,327 | 70 | 65 |
| 고등학교 | 2,407 | 240 | 217 |
| 특수학교 | 202 | 40 | 32 |
| 그 밖 | 307 | | |

### 9.1 특수학교는 세 학급이 하나로 합쳐지고 있었다

특수학교 한 곳이 초등부와 중학부, 고등부를 함께 운영한다. 학년은 과정마다 1부터
다시 센다. 그래서 한 학교에 1학년 1반이 셋 있다.

나이스는 `SCHUL_CRSE_SC_NM` 으로 과정을 준다. 그런데 우리 학급 열쇠에 그 항목이
아예 없었다. 열쇠가 (학교, 학년도, 주야, 계열, 학과, 학년, 반)이라 세 학급이 같은
열쇠를 받는다.

| 재어 본 것 | 결과 |
|---|---|
| (학년, 반)이 여러 과정에 걸치는 학교 | 31 / 32 |
| 한 칸에 여러 과정이 겹치는 자리 | 6,554 |
| 그중 과목까지 서로 다른 자리 | 6,028 |

**오류로 멈추지 않았다.** 엔진이 그 겹침을 분반으로 읽어 유효한 시간표라고 통과시켰다.
숫자를 보면 성립할 수 없다는 것이 바로 드러난다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 학급 수 (중앙) | 13 | 24 |
| 배정 수 (중앙) | 626 | 682 |
| 학급당 배정 | **48.1** | 28.4 |
| 학교당 묶음 (중앙) | 168 | 26 |

한 주는 5일 곱하기 7교시로 35칸이다. 학급당 48.1개는 있을 수 없다.
`SCHUL_CRSE_SC_NM` 을 학급 열쇠에 넣어 고쳤다. 화면 이름도 `초1-1`, `중1-1`, `고1-1`
로 갈라 붙인다. 안 그러면 세 학급이 모두 `1-1` 로 보인다.

**조용히 틀린 자료가 오류보다 나쁘다.** 이 자리는 오류를 내지 않았기 때문에 오래 남았다.

### 9.2 학교 종류가 스물세 가지인데 넷만 보고 있었다

`timetableEndpointOf` 는 이름이 초등으로 시작하면 els, 중학이면 mis, 특수면 sps,
나머지는 전부 his 로 보냈다. 실제로 불러 확인하니 이렇다.

| 학교 종류 | 곳 | 자료를 주는 곳 | 이름만 보면 |
|---|---|---|---|
| 각종학교(중) | 38 | misTimetable | his 로 감 |
| 방송통신중학교 | 24 | misTimetable | his 로 감 |
| 평생학교(중) | 19 | misTimetable | his 로 감 |
| 각종학교(초) | 8 | elsTimetable | his 로 감 |
| 평생학교(초) | 5 | elsTimetable | his 로 감 |

**94곳의 선생님이 시간표가 있는데도 "없습니다"를 본다.**

종류를 하나씩 적어 넣지 않았다. 스물세 가지가 더 늘 수 있고, 각종학교(중) 가운데
실제로 초등 자료를 주는 곳도 있어서 이름으로 하나를 고르는 방식 자체가 맞지 않는다.
대신 순서를 준다. 가장 그럴듯한 것을 먼저 부르고, 다섯 주가 모두 비면 남은 셋을
가장 최근 주로 한 번씩 더 불러 본다.

초중고는 첫 번째에서 끝난다. 전국 12,075곳(96%)이 그렇다. 값을 더 내는 것은
나머지 종류뿐이다.

### 9.3 초등학교에서는 교체가 62% 성립하지 않는다

초등학교는 담임제다. 한 선생님이 그 학급 수업을 거의 다 맡는다.
전담 과목(영어, 체육, 음악, 미술, 과학, 실과 등)만 다른 분이 맡는 모형으로 재었다.

| 학교급 | 교체 후보 0건 | 빈 결과일 때 담당 몫 중앙 | 답이 있을 때 담당 몫 중앙 |
|---|---|---|---|
| 초등학교 | **34.8%** (담임만 보면 61.9%) | **1.00** | 0.50 |
| 중학교 | 0.0% | 0.00 | 0.09 |
| 특수학교 | 0.0% | 0.00 | 0.10 |

담임 선생님은 그 학급 수업을 혼자 다 맡고 계신다. 바꿔 줄 상대가 자기 자신이라
교체가 성립할 수 없다. 구조가 그런 것이지 조건이 까다로워서가 아니다.

그런데 화면은 이렇게 안내하고 있었다.

> 지금 조건에서는 가능한 변경이 없습니다.
> 근무 불가 시간과 묶음 수업을 확인해 주세요.

**둘 다 원인이 아니다.** 엉뚱한 데를 보게 만든다. 초등학교는 전국 6,341곳으로
학교의 절반이다. 그 절반이 세 번에 한 번씩 틀린 안내를 받는다.

담당 몫으로 까닭을 가른다. 기준 0.6에서 빈 결과의 92%가 걸리고 답이 있던 것은
4%만 걸린다. 0.3까지 내려도 설명력은 92% 그대로인데 답이 있던 것이 17%로 늘어
애먼 안내가 붙는다.

> 교체할 수 있는 상대가 없습니다.
> 이 학급 수업을 대부분 직접 맡고 계셔서 서로 바꿀 상대가 없습니다. 보강으로 진행하십시오.

### 9.4 학년마다 하루 교시 수가 다르다

초등학교 1학년은 4교시에 끝나고 6학년은 6교시까지 한다. 그런데 `periodsPerDay` 는
학교 전체로 하나다. 6교시라고 두면 1학년의 5, 6교시가 빈 시간으로 보인다.

| 학교급 | 같은 요일에 학년 간 교시 수가 2교시 이상 벌어지는 학교 |
|---|---|
| 초등학교 | 54 / 57 |
| 중학교 | 1 / 65 |
| 특수학교 | 25 / 32 |

실제로 그 자리로 옮기라는 안이 나오는지 세었다.

| 학교급 | 막기 전 | 막은 뒤 |
|---|---|---|
| 초등학교 | 254건 (6.7%), 53곳 | 114건 (3.1%), 23곳 |
| 중학교 | 21건 (0.3%), 6곳 | 3건 (0.0%), 2곳 |
| 특수학교 | 24건 (0.7%), 12곳 | 5건 (0.2%), 2곳 |

**남은 것은 전부 학급이 하나뿐인 학년이다.** 세 학교급 모두 100%다.
그 경우는 "그 학년에 없는 교시"와 "그저 비어 있는 칸"을 자료로 가릴 수 없다.
6.3절에서 요일을 다룰 때 세운 원칙을 그대로 따른다. 같은 학년 학급 여럿이 몇 주에
걸쳐 한 번도 안 쓴 교시라야 없는 교시로 본다. 학급 하나로는 단정하지 않는다.

이것이 "모르는 것을 없는 것으로 바꾸는" 잘못의 네 번째다. 앞의 셋은 교사 표를 덜
채운 자리(6.2절), 자료가 없는 요일(6.3절), 인증키 없이 잘린 교시(8절)였다.

### 9.5 같은 잘못을 화면 쪽에서 한 번 더 찾았다

8.7절에서 `gradeShapes` 가 학급 키에서 학년을 캐내던 것을 고쳤다.
화면 코드에도 같은 줄이 있었다.

```
const m = /\d+/.exec(result.target.klass);
```

엔진만 고치고 여기를 두었으면 학년 안내는 여전히 안 떴다.
**한 군데서 찾은 잘못은 같은 저장소 안에서 다시 찾아봐야 한다.**

그리고 이 절을 재는 동안 제가 만든 잘못도 하나 있었다. 특수학교 학급 이름을
`초1-1` 로 바꾸고 나니 앞머리 숫자를 읽던 `klassGrade` 가 NaN 이 되어 특수학교
32곳 전부에서 학년 판정이 다시 죽었다. 8.7절과 똑같은 모양이다.
`field-run.test.ts` 가 바로 잡았다. 표시 이름에서 값을 캐내는 짓을 그만두고
나이스가 준 학년을 그대로 들고 가도록 고쳤다.

---

## 10. 엔진을 한 번 더 훑었다

지표와 학교급을 고친 뒤 엔진 자체를 다시 봤다. 셋이 나왔다.

### 10.1 `validate` 는 셋만 보고 있었다

교사 중복, 학급 중복, 근무 불가다. **휴업일과 교시 제한, 담당 미상 자리 겹침,
묶음 무결성, 슬롯 범위는 안 본다.** 그래서 추천안을 적용한 뒤 `validate` 만 돌려
"깨지지 않았다"고 말할 수 없었다. 8절에서 제가 그렇게 말했는데 그 말이 실제보다
강했다.

여덟 가지를 다 보는 검사를 `invariants.test.ts` 에 붙였다.

설계에서 한 가지를 바로잡았다. **"위반이 하나도 없어야 한다"로 쓰면 틀린다.**
시작부터 어긋나 있는 것이 정상이다. 휴업일이 정해져도 나이스 기준 시간표에는 그날
수업이 그대로 있고, 연수로 못 오는 그 시간에 원래 수업이 있어서 결강이 생긴다.
그것을 푸는 것이 이 도구의 일이다. 그래서 **위반이 새로 늘지 않는다**를 본다.

그리고 실제 자료에는 휴업일도 근무 불가도 없다. 그대로 돌리면 그 규칙들이 한 번도
발동하지 않아 검사한 척만 하게 된다. 조건 여섯 가지를 얹어 돌리고, 조건이 실제로
걸렸는지까지 검사한다.

| 재어 본 것 | 결과 |
|---|---|
| 학교 | 371곳 |
| 시나리오 | 2,226개 (그중 조건이 실제로 걸린 것 1,463개) |
| 추천안 | 78,810개 |
| 보강 후보 | 5,415개 |
| 새 위반 / 목적 미달성 / 수업 유실 / 빈 시간 증가 / 비결정성 | 전부 0 |

**"적용하면 그 선생님이 그 시간에서 실제로 빠지는가"를 처음 검사했다.**
이 도구의 존재 이유인데 검사가 없었다.

### 10.2 보강이 학급 단위 휴업을 안 보고 있었다

`coverCandidates` 는 학교 전체 휴업일만 봤다. 1학년이 수학여행으로 빠진 날에도
1학년 보강 후보를 낸다. 교체 쪽은 `closedKlass` 를 이미 보고 있어서 **두 경로가
서로 다른 말을 하고 있었다.**

### 10.3 고등학교 59%가 설정을 끝낼 수 없었다

가장 큰 것이다. 나이스 고등학교 시간표에 반 번호(`CLASS_NM`)가 빈 행이 온다.

```
GRADE: "3", CLASS_NM: null, PERIO: "2", ITRT_CNTNT: "미적분", CLRM_NM: "특별실"
```

**고교학점제 선택과목이다.** 수강생이 여러 학급에서 모이는 강좌라 반 번호를 붙일
자리가 없다. 강의실이 `3탐구`, `2기초` 같은 교과교실로 온다.

| 학교급 | 그런 학교 | 전체 행 가운데 | 한 학교 최대 |
|---|---|---|---|
| 초등학교 | 0 / 57 | 0.0% | 0% |
| 중학교 | 0 / 65 | 0.0% | 0% |
| 고등학교 | **127 / 217 (59%)** | 9.4% | **77%** |
| 특수학교 | 6 / 32 | 0.5% | 3% |

3학년 8,576행, 2학년 5,648행으로 고교학점제 학년에 몰려 있다. 과목도 확률과 통계,
화법과 작문, 미적분처럼 전형적인 선택과목이다.

이것을 결손으로 보고 격리했고, 완전성 관문이 격리 0을 요구했다. 그래서 그런 학교는
이 화면에서 멈춘다.

> **확인이 필요한 행이 있습니다** / 중단
> 일부 공식 행을 온전히 확인하지 못했습니다. 초대 링크와 추천 기능은 잠긴 상태로 둡니다.

**앞으로 갈 길이 없다.** 무엇이 문제인지도 안 알려 준다.

반 번호만 없고 나머지가 다 있으면 결손이 아니라 강좌로 따로 담는다. 완전성 관문은
진짜 결손만 본다. 그리고 무엇인지 화면에 그대로 적는다.

> 학급에 매이지 않는 강좌가 N행 있습니다. 고교학점제 선택과목처럼 수강생이 여러
> 학급에서 모이는 수업이라 반 번호가 없습니다. 자료가 잘못된 것이 아니며 다음
> 단계로 갈 수 있습니다. 다만 어느 학급 학생이 듣는지 알 수 없어 시간표에는 넣지
> 않습니다. 그 수업의 교체와 보강은 이 도구로 다룰 수 없습니다.

**시간표에 넣지는 않는다.** 어느 학급 학생이 듣는지 모르는 채로 넣으면 그 학생들의
다른 수업과 겹치는지 확인할 수 없다. 막는 것과 아는 척하는 것은 다르다.

이 발견으로 3장의 서술 하나가 바뀐다. "고교학점제 선택과목은 이 자료로 볼 수 없다"고
적었는데, **일부는 오고 있었다.** 학급에 안 매여 있을 뿐이다.

### 10.4 이동수업 알림 소음이 문서와 열 배 달랐다

문서에 이렇게 적혀 있었다.

> 묶음이 없는 학급 24개짜리 학교로 재어 보니 수업 768개 가운데 8개에서만 알림이 떴다.
> 백 개에 하나꼴이다. 이 비율을 시험으로 잠가 두었다. 열에 하나를 넘으면 실패한다.

**합성 학교로 잰 값이다.** 실제 학교 371곳으로 재니 이렇다.

| 학교급 | 수업 | 알림 | 비율 | 학교별 최대 |
|---|---|---|---|---|
| 초등학교 (57곳) | 22,298 | 961 | 4.3% | 30% |
| 중학교 (65곳) | 32,637 | 2,319 | 7.1% | 64% |
| 고등학교 (217곳) | 138,862 | 14,784 | **10.6%** | 32% |
| 특수학교 (32곳) | 24,575 | 2,513 | **10.2%** | 26% |

**스스로 정한 실패선 10%를 371곳 가운데 122곳(33%)이 넘는다.** 한 곳은 63.6%다.

까닭이 분명하다. 합성 학교는 학급마다 과목을 흩뿌려 놓아 같은 교시에 같은 과목이
겹치는 일이 드물다. 실제 학교는 한 학년이 같은 시간에 같은 과목을 함께 듣는 편성이
흔하다. 3절에서 "합성 학교가 현실과 달랐다"고 적고 지표를 고쳤는데, 이 자리에서
같은 함정에 다시 빠져 있었다.

동작은 바꾸지 않았다. 이 알림은 막는 것이 아니라 "함께 움직이는 수업입니까"를 여쭙는
버튼이고, 얼마나 자주 여쭤도 되는지는 선생님께 물어야 정할 수 있다. 대신 시험과
코드 주석에 실측값을 함께 적어 다음 사람이 1%를 믿지 않게 했다.

### 10.5 휴업 판정을 실측했고 고치지 않기로 했다

휴업으로 읽힌 (학급, 날) 629건을 종류별로 갈랐다.

| 무리 | 건수 | 예 |
|---|---|---|
| 진짜 교외 활동이나 휴업 | 448 (71%) | 수학여행 106, 수련활동 75, 재량휴업일 67 |
| 교내 창의적 체험활동 | 175 (28%) | 동아리활동 50, 자율·자치활동 41, 기타활동 33 |
| 진짜 과목을 잘못 읽음 | 6 (1%) | 체육 1, 즐거운생활 1, 프로젝트 실무 4 |

잘못 읽은 6건의 원인이 둘이다.

하나는 **학급이 하나뿐인 학년**이다(체육, 즐거운생활). 규칙이 "그 학년 관측 학급
전부가 같은 값"을 요구하는데 학급이 하나면 1/1 이라 그냥 통과한다.
9.4절에서 교시 수를 다룰 때는 "학급 하나로는 단정하지 않는다"로 갔는데 여기는 다르다.

**그래서 여기는 바꾸지 않았다.** 학급 하나짜리 학년에서 이 예외를 없애면 체육 오판
1건은 고치지만 수학여행 날을 수업일로 읽게 된다. 그러면 수학여행 날로 수업을
옮기라는 안이 나온다. 그쪽이 훨씬 나쁘다. 두 방향 모두 틀리는 자리에서는 덜 나쁜
쪽을 고른다.

다른 하나는 **별표 없는 전공 실습**이다. 마이스터고 3학년 네 학급이 하루 종일
"프로젝트 실무"를 하는데 과목명에 별표가 없어 6.1절의 예외를 빠져나간다.
6.1절에서 고쳤다던 잘못이 같은 모양으로 되살아난 것이다.

이름 목록으로 가릴 수 있지만 그 길은 처음부터 안 쓰기로 했다. 구조로 가리려고
"그 과목이 다른 날에도 정규 수업으로 나오는가"를 재어 보니 휴업 판정의 87%는 한 번도
안 나오고 13%는 나왔다. 신호가 있기는 하다. 그런데 프로젝트 실무는 그 학년 네 학급이
전부 휴업으로 분류되어 정규 수업으로 남은 것이 없다. 순환이라 이 신호로는 안 잡힌다.

629건 가운데 6건이고, 휴업일 자체가 전체 학급 날짜의 1.7%다. 지금은 재어 둔 상태로
남긴다. 고치려면 선생님께 "동아리활동 날에 보강이 필요합니까"를 먼저 물어야 한다.

---

## 11. 예상되는 문제를 전부 뽑아 하나씩 쟀다

지금까지는 발견한 것을 쫓아갔다. 이번에는 나올 만한 것을 먼저 늘어놓고 하나씩
실제 자료로 쟀다. 371곳의 시간표와 132곳의 한 학기 학사일정을 썼다.

### 11.1 고친 것

**특수학교 학사일정이 과정마다 따로 온다.** 32곳 가운데 31곳이 유치원, 초등학교,
중학교, 고등학교, 전공과 가운데 셋에서 다섯 과정을 운영하고, 6곳에서는 과정마다
쉬는 날이 달랐다. 유치원 여름방학이 먼저 시작하는 식이다. 휴업일 1,227일 가운데
30일(2%)이 그렇다. 엔진이 `SCHUL_CRSE_SC_NM` 을 안 봐서 유치원만 쉬는 날에 초등부와
중학부 수업까지 쉬는 것으로 읽었다. 9.1절에서 시간표를 고친 것과 같은 종류다.

**해당 학급이 하나도 없는 행사가 학교 전체 휴업이 되던 자리.** 전공과나 유치원처럼
학사일정에는 있고 시간표에는 없는 과정의 행사가 여기 온다. 넓히지 않고 아예 안 만든다.

**예시 학교의 과목명이 실제보다 짧았다.** 문서에 "중앙 6자에 최대 14자"라 적혀
있었는데 14자는 99퍼센타일이고 실제 최대는 35자다.

| 학교급 | 중앙 | p95 | p99 | 최대 |
|---|---|---|---|---|
| 초등학교 | 2 | 7 | 7 | 16 |
| 중학교 | 2 | 9 | 9 | 16 |
| 고등학교 | 5 | 11 | 15 | **35** |
| 특수학교 | 5 | 10 | 17 | 33 |

20자 넘는 과목이 있는 학교가 고등학교 217곳 가운데 20곳이다. 예시 학교의 최장이
14자여서 폰 화면 검사가 실제보다 쉬웠다. 26자짜리로 바꿨고 390px 에서 그대로 통과한다.

### 11.2 재어 보니 문제가 아니었던 것

예상은 했으나 실제로는 괜찮았던 것도 적어 둔다. 안 적으면 다음 사람이 같은 걱정을
다시 한다.

| 걱정한 것 | 실측 |
|---|---|
| 큰 학교에서 추천이 느릴 것 | 추천 1회 최악이 중앙 60ms, 최대 360ms. 1초 넘는 학교 0/371 |
| 긴 과목명이 폰 화면을 깨뜨릴 것 | 26자를 넣어도 390px 에서 잘림 0, 작은 조작 0 |
| 학사일정에 해당 학년이 하나도 없는 행이 있을 것 | 14,458건 가운데 0건 |
| 토요일 수업이 있을 것 | 월~금만 나온다. 요일 5개 가정이 성립한다 |
| 한 학교에 학년도나 학기가 섞일 것 | 371곳 모두 한 가지다 |
| 퇴화한 입력에서 터질 것 | 열 가지를 넣어도 안 터지고 성립하는 답을 낸다 |

퇴화 입력 열 가지는 이렇다. 배정 없음, 없는 수업 선택, 수업 하나, 교시 11개,
반 이름 12자와 과목명 35자, 모든 교사 근무 불가, 모든 요일 휴업, 학급 전체 담당
미상, 나이스 행 하나, 나이스 행 없음.

### 11.3 자료의 끝값

앞으로 무엇을 넣어도 되는지 알아 두려고 끝값을 적어 둔다.

| 항목 | 실측 |
|---|---|
| 교시 | 1부터 **11**까지 (8교시 222행, 11교시 15행) |
| 요일 | 월~금만 |
| 학년 | 1~6 |
| 과목명 | 최대 35자 |
| 반 이름 | 최대 12자, 숫자가 아닌 것 33종 (`4(자유학기, 동아리)`, `믿음`, `배려`) |
| 학과 | 288종 |
| 계열 | 16종 |

### 11.4 한 주만 받으면 이력이 안 나온다

`fromNeis` 는 기준 시간표와 다른 칸을 변경으로, 같은 날 두 칸이 서로의 과목을
맞바꿔 가진 것을 교환으로 본다. 그런데 371곳 전부에서 변경 0건, 교환 0건이 나왔다.

까닭이 단순하다. 앱은 한 주만 받는다. 기준 시간표가 곧 그 주 자체라 어긋날 수가 없다.

같은 학교 20곳을 네 주로 받아 다시 재니 변경 2,955건, 교환 169건이 나왔고 **잘못
짚은 교환은 0건**이었다. 판정한 교환마다 그날 그 학급에서 두 과목이 실제로 자리를
바꿔 있는지 하나씩 확인했다.

기능은 맞다. 다만 지금 앱 경로에서는 값이 안 나온다. 이 값을 쓰는 곳은 CLI 하나이고
화면에는 안 쓴다. 화면에서 쓰려면 여러 주를 받아야 한다.

부수적으로 알게 된 것이 있다. **학교는 시간표를 자주 바꾼다.** 20곳이 네 주 동안
2,955칸을 바꿨다. 학교당 한 주에 37칸꼴이다.

---

### 11.5 방학에 열면 지난 학기 시간표가 온다

오늘이 2026년 8월 20일이다. 앱이 하는 그대로 최근 5주를 거슬러 보며 실제 학교
120곳을 재었다.

| | |
|---|---|
| 시간표를 찾은 학교 | 116 / 120 (97%) |
| 현재 주(8/17)에서 찾음 | 94곳 |
| 1~2주 전에서 찾음 | 7곳 |
| **4주 전(7/20)에서 찾음** | **15곳** |

방학이라 아무것도 안 나올 줄 알았는데 97%가 찾는다. 대부분 2학기가 시작됐다.

문제는 되돌아간 학교다. **4주 전을 찾은 15곳과 3주 전 1곳이 모두 1학기였다.**
학기가 바뀌면 과목도 교사도 교시 수도 달라진다. 1학기 시간표로 2학기 교체를
계획하면 통째로 틀린다.

**그런데 날짜로 학기를 짐작하면 안 된다.** 방학 길이가 학교마다 달라 같은 주에도
학기가 갈린다.

| 되돌아간 주 | 1학기 | 2학기 |
|---|---|---|
| 0주 (현재 주) | **47곳** | **47곳** |
| 1주 | 6곳 | 0곳 |
| 2주 | 1곳 | 0곳 |
| 4주 | 15곳 | 0곳 |

현재 주를 찾은 94곳이 정확히 반반이다. "8월 중순이면 2학기"라는 규칙을 넣었으면
절반을 틀렸을 것이다.

그래서 판정하지 않는다. **사실만 적는다.** 자료 점검 화면에 학기를 그대로 띄우고,
현재 주가 아니면 몇 주 전 것인지 밝힌다.

> 이 시간표는 4주 전 주간입니다. 그 뒤로는 공개된 수업이 없어 가장 최근 수업 주를
> 가져왔습니다. 방학을 건너뛰면 지난 학기 시간표일 수 있습니다. 위의 학기 표시를
> 확인하십시오.

몇 학기 것이고 몇 주 전 것인지는 선생님이 보면 바로 아신다. 도구가 대신 판정하려다
절반을 틀리는 것보다 낫다.

### 11.6 야간 과정과 특이 학교 종류

표본에 없던 아홉 종류를 따로 받았다. 방송통신고와 중, 평생학교(고)와 (중),
각종학교(고)와 (중)과 (초), 고등기술학교다. 53곳 가운데 45곳이 시간표를 공개한다.

| 종류 | 자료 | 엔드포인트 | 주야 | 교시 |
|---|---|---|---|---|
| 방송통신고등학교 | 5/6 | his | 주간 | 1~7 |
| 평생학교(고)-2년6학기 | 6/6 | his | **야간, 주간** | 1~6 |
| 평생학교(고)-3년6학기 | 5/6 | his | 주간 | 1~7 |
| 각종학교(고) | 4/6 | his | 주간 | 1~8 |
| 고등기술학교 | 3/5 | his | 주간 | 1~7 |
| 방송통신중학교 | 4/6 | mis | 주간 | 1~7 |
| 평생학교(중)-2년6학기 | 6/6 | mis | **야간, 주간** | 1~6 |
| 각종학교(중) | 6/6 | mis | 주간 | 1~10 |
| 각종학교(초) | 6/6 | **els 와 mis 둘 다** | 주간 | 1~9 |

**평생학교는 한 학교에 주간과 야간이 섞여 있다.** 학급 열쇠에 주야가 들어 있어
갈리지만, 화면 이름이 둘 다 `1-1` 이면 9.1절의 특수학교와 같은 문제가 된다.
재어 보니 **45곳 가운데 0곳**이다. 주간과 야간이 다른 반 번호를 쓴다. 문제가 아니다.

각종학교(초)가 els 와 mis 양쪽에서 자료를 준다. 9.2절에서 엔드포인트를 하나로
고르지 않고 순서를 준 이유가 여기 있다.

이 45곳을 엔진에 통과시켰다. 시나리오 270개, 추천안 5,706개, 보강 후보 88개,
**새 위반 0건**이다.

### 11.7 저장 크기

가장 큰 학교도 0.30MB 다. `localStorage` 한도인 5MB 를 넘는 학교는 371곳 가운데
0곳이다. 중앙 0.10MB, 95퍼센타일 0.24MB 다.

---

## 12. 엔진은 멀쩡한데 화면이 그 값을 안 넘기고 있었다

11절까지는 엔진만 재고 있었다. 실제 자료 371곳을 통과시켜 결함을 잡고 고쳤다.
그런데 화면에서 쓰는 길이 엔진에 무엇을 넘기는지는 한 번도 안 봤다.

봤더니 두 길이 따로 있었다.

| 길 | 만드는 곳 | 지금 화면에 쓰이나 |
|---|---|---|
| 나이스 자료를 직접 읽는 길 | `packages/engine/src/adapters/neis.ts` | 아니다 |
| 작업 공간에서 만드는 길 | `apps/web/lib/resolution.ts` | 그렇다 |

11절까지 고친 것은 대부분 첫 번째 길이다. 화면은 두 번째 길로 돈다.

### 12.1 못 닿는 화면을 고치고 있었다

`Workbench.tsx` 안의 `LegacyWorkbench` 는 1,424줄이다. 격자, 근무 불가 지정,
수업 없는 요일 지정, 추천 패널이 다 여기 있다. 그런데 화면에 뜨지 않는다.

`RoleWorkbench` 가 teacher, ops, class 세 갈래를 모두 제 컴포넌트로 보내고,
`AppShell` 은 landing 과 setup 을 직접 처리해 `RoleView` 를 아예 부르지 않는다.
`RoleViewAdapterProps['location']` 이 그 둘을 빼 놓은 타입이라, 세 갈래를 처리한
다음 자리에서 타입이 `never` 로 좁혀진다.

이것을 읽어서 안 것이 아니다. 근무 불가 저장이 안 되는 것을 찾아 고쳤는데 화면이
그대로였다. 그러고 나서야 봤다.

주석만 두면 조용히 거짓말이 된다. `const noRoleLeft: never = props.location` 을
넣어 타입 검사가 확인하게 했다. 갈래를 빼거나 새 역할을 더하면서 여기를 안 고치면
타입 검사가 깨진다.

지우지는 않았다. 이 안에 화면에서 쓰는 길에 아직 없는 것이 들어 있다.

| 기능 | 지금 화면에 있나 |
|---|---|
| 근무 불가 지정 (시간강사, 육아시간) | 없다 |
| 수업 없는 요일 지정 (정기고사, 학교 행사) | 없다 |
| 자료가 빠진 요일 경고 | 없다 |
| 이동수업 안내 (`gradeShapes`) | 없다 |
| 동명이인 경고 | 없다 |

이 다섯은 화면을 새로 만들어야 하는 자리라 이번에 손대지 않았다. 다만 어디에
없는지는 적어 둔다. 다음에 이 화면을 만들 사람이 옛 것을 다시 발명하지 않도록.

### 12.2 자료가 없는 날로 옮기라고 하고 있었다

화면에서 쓰는 길은 `config.periodsPerDay` 도 `klassPeriodsPerDay` 도 안 넘겼다.
그 주에 며칠치 자료가 왔는지, 그 요일이 몇 교시까지인지를 엔진이 알 길이 없었다.

재어 보니 나이스가 한 주를 다 주지 않은 주에 도구는 자료가 없는 금요일로 수업을
옮기라고 했고, **그 안이 맨 위에 왔다.** 모르는 자리를 빈 자리로 세는 잘못이며,
이 저장소에서 되풀이되는 모양이다. 이것으로 여섯 번째다.

고친 뒤 같은 자료에서 그 안이 사라졌고, 멀쩡한 월, 수, 목 자리는 그대로 남았다.
막는 규칙이 멀쩡한 자리까지 막으면 아무 안도 못 내고, 그것이 더 나쁜 고장이다.

학년별 값은 엔진이 나이스 자료에 쓰는 규칙을 그대로 옮겼다. 학급이 하나뿐인
학년에는 안 붙인다. 학급 하나만 보면 그저 비어 있는 칸과 그 학년에 없는 교시를
가릴 수 없기 때문이다.

### 12.3 부담 균형이 늘 0이었다

엔진은 `recentBurden` 으로 교체 점수와 보강 순위를 깎는다. 세 번 연달아 부탁받은
분이 한 번도 안 받은 분과 같은 자리에 오르면 도구를 오래 쓸 수 없기 때문이다.

화면에서 쓰는 길은 이 값을 한 번도 넘기지 않았다. `cover.ts` 와 `score.ts` 의
부담 감점이 늘 0으로 계산되고 있었고, "최근에 이미 맡으셨습니다" 경고도 한 번도
뜨지 않았다. 코드는 있고 검사도 통과하는데, 실제로는 아무 일도 안 하고 있었다.

승인된 사건에서 세어 넘기게 했다. 무게는 옛 화면이 쓰던 것을 그대로 옮겼다.
보강은 두 번, 교체는 한 번, 결강 당사자는 안 센다. 기간은 이 주와 앞의 세 주다.

### 12.4 하루치 부재를 이어서 푸는 것을 안 재고 있었다

실제 자료 검사는 매번 원본에서 다시 시작해 한 건만 풀었다. 실제로 쓰는 모양은
다르다. 연수로 하루를 비우면 그날 수업이 대여섯 개고, 일과 담당은 앞에서 적용한
결과 위에서 다음을 찾는다.

154곳으로 이어 풀기 검사를 돌렸다. 한 사람이 하루를 비우는 경우와 두 사람이 같은
날 함께 비우는 경우를 본다. 통과했다. 근무 불가 검사를 일부러 빼자 위반 474건과
남겨 둔 결강 24건이 나왔다. 검사가 헛돌지 않는다.

### 12.5 결강난 자리가 비어 보였다

부재를 낼 때 그 기간의 수업을 골라 담는다. 담긴 수업의 그 교시는 그분이 못 맡는
자리다. 그런데 화면에서 쓰는 길은 `input.unavailable` 을 늘 비워 두고 있었다.

그래서 결강난 1교시 수업을 같은 분의 결강난 3교시 자리로 옮기라는 안이 나올 수
있었다. 3교시 수업도 함께 옮기는 계획이면 그 자리가 비어 보여 학급 충돌 검사에도
안 걸린다. 옮겨 봐야 두 자리 다 그분이 못 맡는 자리다.

처음에는 부재 **날짜 전체**를 막았다. 기존 검사 열두 건이 깨졌다. 부재 작성 화면에
"하루 종일" 선택이 따로 있고, 회의처럼 두어 시간만 비우는 부재가 정상 경로였다.
자료가 확실히 말해 주는 것은 담긴 수업의 그 교시뿐이라 거기까지만 막았다.

검사가 고침을 되돌려 준 자리다. 넓게 막는 쪽이 안전해 보였지만 자료가 그만큼
말해 주지 않았다.

### 12.6 이미 게시된 변경이 격자에 없었다

`activeLessons` 는 학교가 준 원래 시간표다. 지난주에 낸 변경이 게시되어 3교시 수업이
4교시로 옮겨 갔어도 거기서는 그대로 3교시다. 예시 자료로 확인하니 엔진이 보는 자리가
3교시, 게시된 자리가 4교시였다.

그 격자로 다음 안을 찾으면 이미 비어 있는 3교시는 차 있다고 보고, 이미 찬 4교시는
비었다고 본다. 뒤의 충돌 검사가 잘못된 안을 걸러 내기는 한다. 그러나 엔진은 그 전에
다섯 개를 골라 순위를 매긴다. **틀린 격자로 고른 다섯**이라 좋은 자리를 아예 못 보고
지나간다. 걸러 내는 것만으로는 못 고치는 종류다.

한 주에 결강이 한 건뿐인 학교는 없다. 마흔 명 학교면 한 주에 몇 건씩 난다. 앞 건이
게시된 뒤 다음 건을 푸는 것이 보통이다.

게시된 것만 얹었다. 결재 중인 변경은 아직 확정이 아니고, 지금 풀고 있는 사건 자신의
변경까지 얹으면 두 번 적용된다.

### 12.7 아침 9시 전에는 어제가 오늘이었다

일과 담당 화면과 학급 공개 화면이 오늘을 `new Date().toISOString().slice(0, 10)` 으로
구하고 있었다. 그것은 UTC 날짜다. 한국은 UTC+9 라서 자정부터 아침 9시까지는 어제가
나온다. **선생님이 1교시 전에 시간표를 확인하는 시간대가 정확히 거기다.**

교사 화면은 지역 날짜를 쓰고 있었다. 그래서 아침마다 두 화면이 서로 다른 날을
오늘이라고 불렀다.

검사가 이것을 못 잡은 이유가 있다. 컨테이너 기본 시간대가 UTC 라 지역 날짜와 UTC
날짜가 같았다. `test/setup-timezone.ts` 로 `Asia/Seoul` 을 걸었다. 쓰는 사람이 있는
시간대에서 재야 이런 것이 보인다. 기존 290건은 그대로 통과했다.

### 12.8 전문교과 실습 표시가 설정 단계에서 버려졌다

나이스는 전문교과 실습 과목명 앞에 별표를 붙여 준다. 학과 전용 실습실에서 그 학과
교사만 맡는 수업이라 아무나 대신 들어갈 수 없고, 엔진은 이 표시로 보강 후보에서
전공 교사를 가린다.

그런데 작업 공간의 `Lesson` 에 이 항목이 아예 없었다. 설정 단계에서 표시가 버려져
엔진의 `proTeachers` 가 늘 비었다. 특성화고 10곳은 수업 칸의 14~34%가 이 표시인데,
그 학교에서 전공이 아닌 분이 실습 보강 후보로 나오고 있었다. 일반고 12곳은 0%라
일반고만 보고 있으면 끝까지 안 보인다.

### 12.9 재량휴업일이 과목이 되어 있었다

학교가 쉬는 날에도 나이스는 그 교시 자리에 "재량휴업일", "기타활동" 같은 문구를
넣어 돌려준다. 화면에서 쓰는 길은 그것을 그대로 수업으로 만들고 있었다.

설정 화면이 "재량휴업일"의 담당 교사를 채우라고 한다. 154곳 가운데 5곳에 이런 자리가
있었고, 오산중학교는 고른 주 닷새 가운데 나흘이 그런 날이었다. **그 학교는 설정을
끝낼 수 없었다.**

엔진은 이미 가려 놓았다. 그 학급 그날의 칸이 모두 같은 값이고 같은 학년의 다른
학급도 모두 그러면 휴업으로 본다. 화면 쪽이 그 판정을 안 보고 있었다.

빼도 되는지 먼저 쟀다.

| | 건수 |
|---|---|
| 휴업 판정이 그 학급의 하루 전체 | 51 |
| 일부 교시만 걸린 경우 | 0 |

하루 종일 체육인 초등학교 한 학급도 여기 걸린다. 그것도 그날 그 학급의 전부라
정규 수업으로 보기 어렵다. 그래서 수업에서 빼고 쉬는 날로 옮겼다.

### 12.10 도움을 받은 쪽이 도와준 쪽으로 뒤집혀 보였다

관제판의 부담 경고는 최근에 자리를 내어 준 분을 세어 올린다. 그런데 결강 당사자까지
세고 있었다. 빈 교시 이동은 바뀌는 수업이 당사자 것 하나뿐이라, 부재를 세 번 낸 분이
"협조 3건"으로 잡혀 부담 경고에 올랐다.

엔진에 넘기는 부담 값은 당사자를 빼고 세고 있었다. 같은 규칙이 두 곳에 따로 있었고
한쪽만 맞았다. 12절 내내 나온 모양이 여기서도 나왔다.

고치다가 한 번 더 배웠다. 처음에는 "한 해결안에서 같은 분이 여러 번 나와도 한 번"
으로 바꿨는데 기존 검사가 막았다. 그 값의 이름이 협조 **건수**이고 화면도 건수로
읽는다. 수업 셋을 옮기는 것은 하나를 옮기는 것보다 무겁다. 검사가 맞았다.

### 12.11 한 건씩 찾는 대신 그물을 뒀다

12절에서 찾은 것이 열 가지다. 전부 눈으로 하나씩 찾았다. 왜 이렇게 몰려 있었는지
이유가 하나다.

**지금까지의 실제 자료 검사가 전부 엔진 어댑터로만 들어갔다.** `neisToTimetable` 에
371곳을 통과시켰지, 화면이 부르는 `createWorkspaceFromNeis` 와
`resolutionRowsForLesson` 으로는 실제 학교 자료를 한 번도 안 넣어 보았다.

그래서 `apps/web/test/field-live.test.ts` 를 뒀다. 실제 학교 자료로 작업 공간을
만들고, 그 주에 수업이 가장 많은 교사가 하루를 비우는 사건을 넣고, 화면이 부르는
그 함수로 안을 받아 본다.

| 무엇을 보는가 |
|---|
| 그 주 자료에 없는 날로 보내지 않는가 |
| 그날 아무 학급도 안 쓰는 교시로 보내지 않는가 |
| 그 학급이 쉬는 날로 보내지 않는가 |
| 그 학급이 이미 수업 중인 자리로 보내지 않는가 |
| 결강으로 담긴 교시로 그분 수업을 되돌리지 않는가 |
| 휴업 문구가 과목으로 남아 있지 않은가 |

오늘 고친 것을 하나씩 되돌려 실제로 걸리는지 확인했다. 요일별 교시 제한을 빼면
7건, 휴업일 거르기를 빼면 오산중학교가 걸린다.

**그리고 이 그물이 곧바로 새 결함을 하나 잡았다.**

소가야중학교는 월요일이 2교시부터 시작해 1교시가 아예 없다.

| 날짜 | 있는 교시 |
|---|---|
| 6월 8일 (월) | **2~7교시** |
| 6월 9일 (화) | 1~7교시 |
| 6월 10일 (수) | 1~7교시 |
| 6월 11일 (목) | 1~7교시 |
| 6월 12일 (금) | 1~5교시 |

`periodsPerDay` 는 "그날 몇 교시까지인가"만 말한다. 위쪽만 막는 값이라 앞이 비는
것은 못 잡는다. 다른 요일에 1교시가 다 있어서 학교 전체 값으로도 안 걸렸고, 도구가
월요일 1교시로 수업을 옮기라고 했다.

그날 학교 전체로 아무 학급도 안 쓴 교시를 `klassBusy` 로 넘겨 막았다. 한 학급만
비어 있는 것은 그냥 공강이라 옮겨도 되는 자리이므로 막지 않는다.

이것이 그물을 둔 값이다. 열 번째까지는 눈으로 찾았고 열한 번째는 검사가 찾았다.

### 12.12 그물이 놓친 것을 브라우저 검사가 잡았다

12.11 의 고침을 넣고 실제 자료 154곳과 217곳을 통과시켰다. 단위 검사 313건도 통과했다.
그대로 올렸더니 **브라우저 검사가 막았다.** 정정 게시 여정에서 정정 사건의 후보가
하나도 안 나왔다.

두 가지가 겹쳐 있었다.

| 무엇 | 왜 틀렸나 |
|---|---|
| 교시 구조를 게시된 자리로 셌다 | 3교시 수업을 4교시로 옮겨 게시하면 3교시가 비는데, 우리가 옮겨서 빈 자리를 "그날 없는 교시"로 읽었다 |
| 학급 하나만 보고 없는 교시라고 했다 | 그 학급이 그때 공강일 뿐일 수 있다. 수업이 하나뿐인 자료에서 1, 2교시가 통째로 막혔다 |

첫째는 원래 자리와 게시된 자리를 **둘 다** 있는 교시로 세어 고쳤다. 원래 자리만 세도
안 된다. 그러면 게시로 옮겨 간 자리가 없는 교시가 되어 정작 거기 있는 수업이 규칙을
어기는 자리에 앉는다. 둘째는 학급 둘 이상이 그날 수업을 가지고 있을 때만 보도록 했다.
학급별 교시 제한에 쓰는 것과 같은 근거 기준이다.

**실제 자료 371곳으로도 못 잡는 것이 있다.** 수업이 하나뿐인 퇴화 자료가 실제 학교
표본에는 없기 때문이다. 예시 자료는 일부러 그런 모양을 만들어 두었고, 브라우저 검사가
그것을 통과시킨다. 그래서 층이 여럿 필요하다.

| 층 | 무엇을 잡는가 |
|---|---|
| 단위 검사 | 값 하나의 계산 |
| 실제 자료 371곳 | 진짜 학교에서만 나오는 모양 |
| 예시 자료 브라우저 검사 | 퇴화 자료와 화면 전체 흐름 |

셋 중 하나만 있으면 나머지 둘이 잡는 것을 놓친다.

한 가지 더 적어 둔다. 이 결함은 **스모크를 안 돌리고 올려서** 나갔다. 실제 자료와
단위 검사가 다 통과해서 그것으로 됐다고 본 것이다. 로컬에서 스모크를 돌렸으면 올리기
전에 잡혔다. 실제로 나중에 같은 빌드로 돌려 보니 똑같이 걸렸다.

### 12.13 여기서 배운 것

**엔진을 아무리 재도 화면이 그 값을 안 넘기면 소용이 없다.** 11절까지 고친 것 가운데
학급별 교시 제한은 화면에 닿지 않고 있었다. 검사가 통과한 것은 검사도 엔진만 보고
있었기 때문이다.

더 크게 말하면 이렇다. **검사가 닿지 않는 길에 결함이 쌓인다.** 열한 가지가 한 자리에
몰려 있던 것은 우연이 아니라 그 길에 검사가 없었기 때문이다. 그래서 결함을 하나씩
고치는 것으로 끝내지 않고, 그 길로 실제 자료가 흐르는 검사를 함께 뒀다.

그래서 이제 두 겹으로 검사한다.

첫째, 화면에서 쓰는 길이 엔진에 넘기는 **값 자체**를 본다. `week-shape.test.ts`,
`burden.test.ts`, `absence-slots.test.ts`, `published-grid.test.ts`,
`today.test.ts`, `pro-marker.test.ts`, `timetable-holiday.test.ts` 다.

둘째, 실제 학교 자료를 **화면이 쓰는 길 그대로** 흘려 본다.
`field-live.test.ts` 다. 이쪽이 그물이고, 앞으로 같은 종류가 생기면 여기서 걸린다.

고침을 하나씩 되돌려 검사가 실제로 깨지는 것을 확인했다. 되돌리지 않고 통과하는
검사는 검사가 아니다.

| 안 넘기던 값 | 그래서 생긴 일 |
|---|---|
| `config.periodsPerDay` | 자료가 없는 날로 옮기라는 안이 맨 위에 왔다 |
| `klassPeriodsPerDay` | 그 학년에 없는 교시가 빈 자리로 보였다 |
| `recentBurden` | 부담 균형 감점이 늘 0이었다 |
| `unavailable` | 결강난 자리가 비어 보였다 |
| 게시된 변경 | 이미 옮겨 간 수업을 옛 자리에서 보았다 |
| 전문교과 실습 표시 | 특성화고에서 전공 아닌 분이 실습 보강 후보로 나왔다 |
| 시간표에 온 휴업일 | "재량휴업일"의 담당 교사를 채우라고 했다 |
| 지역 날짜 | 아침 9시 전에 어제를 오늘이라고 불렀다 |
| 부담 셈법 | 부재를 낸 분이 협조를 많이 한 분으로 잡혔다 |
| 그날 앞이 비는 교시 | 월요일 1교시가 없는 학교에 그 자리로 옮기라고 했다 |

---

## 13. 한 학교를 학기처럼 굴려 보았다

12절까지는 사건 하나만 봤다. 실제 학교는 한 주에도 여러 건이 나고, 앞 건이 게시된
위에서 다음 건을 푼다. 쌓였을 때 시간표가 여전히 성립하는지는 한 번도 안 봤다.

부재 접수부터 게시까지 화면이 부르는 그 함수들로 그대로 돌리는 시뮬레이션을 뒀다.
`apps/web/test/simulation.test.ts` 다.

### 13.1 절반이 승인에서 막혔다

첫 결과다.

| | 학교 12곳 |
|---|---|
| 게시 성공 | 47건 |
| 승인 관문에서 막힘 | **48건** |
| 막힌 이유 | 교사 중복 111, 학급 중복 104, 담당 미확정 62 |

하루를 비우면 결강이 대여섯 개고 일과 담당은 하나씩 고른다. 그런데 **각 결강의 후보를
서로 모르는 채로** 만들고 있었다. 두 결강이 같은 자리를 노리는 안이 나란히 추천되고,
둘 다 고른 뒤에야 승인이 막힌다. 그때 화면은 무엇을 무르고 다시 골라야 하는지 말해
주지 않는다.

이미 고른 안을 격자에 얹고 막을지도 함께 보게 고쳤다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 게시 성공 | 47 | **91** |
| 승인 막힘 | **48** | **4** |

남은 넷은 정당한 막힘이다. 결강 하나에 안이 없으면 사건 전체를 승인할 수 없다.

### 13.2 주마다 다시 불러오면 지난 기록이 사라졌다

학교는 주마다 나이스를 다시 받는다. 그때 작업 공간을 통째로 새로 만들면서 사건과
게시, 감사 기록을 전부 빈 배열로 두고 있었다. 저장은 같은 열쇠에 덮어쓴다.
**한 주를 쓰고 다음 주 시간표를 받는 순간 지난주에 누가 무엇을 결재했는지가 사라진다.**

결재가 있는 도구에서 감사 기록이 사라지는 것은 기능 하나가 없는 것과 다르다.

이어 가게 고쳤다. 그런데 그대로 이어 붙이면 저장 한도를 넘는다. 가장 큰 학교로 쟀다.

| 주차 | 저장 크기 |
|---|---|
| 1 | 0.52MB |
| 8 | 4.16MB |
| 10 | **5MB 넘음** |
| 20 | 10.40MB |

`localStorage` 한도가 5MB 라 학기 중반에 저장이 막힌다. 사건과 감사 기록은 작아서 다
남기고 수업만 이번 주와 바로 앞 주를 남긴다. 스무 주를 이어도 1MB 안쪽이다.

고친 뒤 새로 생긴 자리도 함께 쟀다. 결재 중이던 사건이 남아 있는데 그 안은 지난주
시간표로 계산한 것이다. 승인이 막히는지 확인했고, 지난주 게시가 이번 주 격자에
얹히지 않는 것도 확인했다.

### 13.3 사건 화면이 7초씩 멎고 있었다

일과 담당 사건 화면은 결강마다 후보를 센다. 목록을 그리면서 한 번, 고른 것을 찾으면서
또 한 번 불렀고 그리기마다 처음부터 다시 셌다.

가장 큰 학교에서 한 건이 240ms 다. 결강 열여섯 건이면 그리기 한 번에 7초가 넘는다.
**반려 사유를 한 글자 칠 때마다 그만큼 멎는다.** `useMemo` 로 상태가 바뀔 때만 세게
했다. 한 사건 여는 데 190~570ms 다.

### 13.4 검사 쪽이 틀린 것도 하나 나왔다

경주고등학교에서 학급 중복 22건이 나왔다. 확인해 보니 검사 쪽 잘못이었다. 선택과목과
수준별 분반은 같은 학급 같은 교시에 여러 강좌가 함께 열리고 나이스도 그렇게 준다.
엔진은 묶음을 한 몸으로 다루는데 검사가 그것을 안 보고 세고 있었다.

**결함이 나왔다고 바로 제품을 고치면 안 된다.** 실제 자료가 그런 모양인지 먼저 본다.

### 13.5 지금 수치

세 가지 시뮬레이션을 실제 학교 자료로 돌린 결과다.

| 시뮬레이션 | 규모 | 깨진 시간표 | 사라진 수업 | 저장 왕복 |
|---|---|---|---|---|
| 누적 운영 | 110곳, 게시 976건 | 0 | 0 | 0 |
| 정정 재게시 | 110곳, 정정 110건 | 0 | 0 | 0 |
| 여러 날 부재 | 110곳, 329일치 | 0 | 0 | 0 |

승인이 막히는 비율은 11% 언저리다. 14.10절을 고친 뒤 다시 재니 72건 가운데 1건이다. 전부 "결강 하나에 안이 없어서"다. 그 결강은 보강도
후보가 없는 자리이고, 손으로 처리해야 한다. 다만 이 수치는 교사 표를 지어 넣고 잰
것이라 실제보다 빡빡할 수 있다. 사람이 채운 표로 재 본 적은 아직 없다.

---

## 14. 주가 넘어가면 무슨 일이 생기는가

13절까지는 한 주 안에서만 굴렸다. 실제 학교는 월요일 아침마다 새 주를 불러오고, 지난
주에 걸쳐 있던 일이 그대로 남는다. 그 지점을 한 번도 안 봤다.

여기서 나온 것은 모두 한 뿌리다. **개정판을 "지금 화면에 띄운 주"로 읽고 있었다.**
개정판은 한 주씩 오는데 사건은 그 사건의 주에 속한다. 둘을 같은 것으로 다루면 주가
넘어가는 순간 어긋난다.

### 14.1 승인까지 간 사건이 월요일 아침에 죽었다

금요일 오후에 부재가 올라와 승인이 났다. 행정 마감이 남아 주말을 넘긴다. 월요일 아침에
이번 주 시간표를 불러오면 활성 개정판이 바뀐다.

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 다시 불러온 뒤 게시까지 감 | 막힘 | 감 |
| 그 사건의 후보를 다시 셈 | 0개 | 5개 |
| 지난주 게시분을 이번 주에 정정 | 0개 | 5개 |

두 군데가 함께 막았다. 검증이 `staleRevision` 을 세워 게시를 막고, 다시 계산하려 해도
후보를 만들 때 보는 수업이 이번 주 것뿐이라 그 사건의 결강 수업이 아예 안 보였다.
막다른 길이었다. 승인까지 간 사건을 되살릴 방법이 없었다.

수업은 지난주 것까지 이미 남겨 두고 있었다. 남겨 두는 이유를 코드에 "게시한 변경을
그 주 안에 정정하는 일이 흔해서"라고 적어 두기까지 했다. 남아 있는데 아무도 안 보는
것이 문제였다. 그 정정 경로는 열린 적이 없다.

새로 만든 안에 찍는 개정판도 활성 개정판이었다. 지난주 사건을 다시 풀면 만들자마자
"다시 계산해야 함"이 되어 모든 안이 막혔다.

### 14.2 부담 경고가 월요일마다 0으로 돌아갔다

같은 뿌리다. 협조 건수를 활성 개정판으로 걸렀는데, 개정판이 한 주씩 오므로 그것은
"이번 주만"과 같은 말이었다.

경고 문턱이 3건이다. 한 주 안에 세 번 도운 사람만 잡히고, 세 주에 걸쳐 다섯 번 도운
사람은 한 번도 안 잡혔다. 엔진에 넘기는 부담값은 네 주로 세고 있어서, 같은 "최근"이라는
말이 두 화면에서 다른 기간을 가리켰다.

주 계산이 두 파일에 따로 적혀 있어서 생긴 어긋남이다. `apps/web/lib/week.ts` 하나로 모았다.

### 14.3 반려된 요청이 재신청을 막았다

중복 검사가 상태를 안 봤다. 반려된 요청도 "이미 있는 요청"으로 세는 바람에, 반려당한
교사가 같은 부재를 다시 낼 수 없었다.

반려 사유를 적는 칸의 안내 문구가 "다시 조정해야 하는 이유"다. 다시 조정하라고 해 놓고
다시 낼 길을 막았다. 화면은 "날짜 또는 수업 선택을 바꾸십시오"라고 안내하는데, 출장
날짜는 이미 정해져 있어 바꿀 수 있는 사실이 아니다.

같은 자리에서 상태 이름이 영문 그대로 나가고 있었다. "rejected 상태의 요청이 이미
있습니다"로 뜬다.

### 14.4 승인만 나고 게시 전인 사건이 잡은 자리

행정 마감에 하루가 걸린다. 그 사이에 다음 사건을 푸는 일이 흔한데, 앞 사건이 가기로
정해 둔 자리를 비어 있다고 보면 일과 담당이 그 자리를 골랐다가 승인에서 막힌다.

**실제 학교 자료로는 이것을 못 쟀다.** 빈 자리가 넉넉해서 두 사건이 같은 자리를 다투는
일이 안 생긴다. 25곳으로 돌려 본 열네 쌍이 모두 통과했고, 예약 규칙을 빼도 결과가
같았다. 아무것도 못 가리는 검사라 지웠다.

다투는 자리를 손으로 지어야 했다. 1학년 1반이 3교시가 비어 있고 1학년 2반은 3교시를
쓴다. 두 교사가 각각 부재를 내고 둘 다 그 자리로 간다. 이 상태에서 예약 목록을 건드리면
검사가 깨진다. 결과는 지금 규칙이 맞다는 것이다. 다만 **검사가 없어서 맞는지 몰랐다.**

### 14.5 한 주가 통째로 쉬는 주

방학이나 수능 휴업 주를 고르면 나이스가 닷새 모든 칸에 "재량휴업일"을 넣어 돌려준다.
휴업 걸러 내기가 그것을 다 빼므로 수업이 하나도 안 남는다. 그런데 받은 행 수는 온전하니
"완전하게 불러왔다"로 표시됐다. 설정이 그대로 끝나고 사람은 텅 빈 시간표를 정상으로 본다.

완전성 판정을 설정 화면과 개정판 표시가 각자 갖고 있었던 것도 함께 고쳤다. 같은 식이
두 군데 있으면 한쪽만 고치게 된다.

### 14.6 수업 번호가 행 순서인 것을 쟀다

수업 번호가 `neis-{행 순서}` 다. 나이스가 준 배열에서 몇 번째인가로 붙인다. 학교가 그
사이 시간표를 손대 행 하나가 끼면 뒤 번호가 모두 한 칸씩 밀리고, 진행 중인 사건이
가리키던 수업이 다른 수업이 된다. 같은 주를 다시 부르면 개정판 번호도 같아서 지금의
어긋남 검사에 안 걸린다.

재 보았다.

| 잰 것 | 학교 | 결과 |
|---|---|---|
| 같은 주를 연달아 두 번 부름 | 12곳 (최대 1,367행) | 12곳 모두 행과 순서가 같음 |
| 나흘 전에 받아 둔 주를 다시 부름 | 371곳 | **371곳 모두 행과 순서가 같음** |

지난 주에 대해서는 나이스 응답이 그대로다. 지금 방식이 실제로 버틴다.

다만 이것은 **이미 지나간 주**를 잰 것이다. 진행 중인 주를 학교가 고치는 경우는 못
쟀다. 그 경우가 남은 위험이고, 15절에 적었다.

### 14.7 반려 사유를 아무도 안 읽고 있었다

교사 화면에 자기 요청이 하나도 안 보이고 있었다. 제출하면 "요청을 제출했습니다"가 한
번 뜨고 끝이다. 게시되기 전까지는 시간표에도 표시가 안 나고, 반려된 요청은 끝까지
안 보인다.

반려 사유가 특히 그렇다. 일과 담당은 사유를 반드시 적어야 한다. 안 적으면 반려 자체가
안 되게 막아 두었다. 그런데 그 값을 읽는 화면이 어디에도 없었다. 코드 전체에서
`rejectionNote` 를 읽는 자리는 저장하는 곳과 옛 자료를 옮기는 곳뿐이다. 적으라고
막아 놓고 아무도 안 읽는 값이었다.

거두는 길도 없었다. `cancelled` 가 상태 목록과 종결 목록에 들어 있는데 거기로 가는
전이가 안 열려 있다. 옛 화면에는 "요청 취소" 단추가 있었고 사건 방식으로 옮기면서 그
길만 빠졌다. 잘못 낸 요청을 거둘 방법이 없으니, 일과 담당이 남의 실수에 반려 사유를
대신 적어 주는 수밖에 없었다.

이것은 실제 자료로도 시뮬레이션으로도 안 잡힌다. 함수는 다 제대로 돌고 있었다.
**부르는 화면이 없다는 것**이 결함이었다. 사람이 겪는 순서대로 따라가 봐야 보인다.

### 14.8 날짜가 겹치는 요청

출장이 하루 늘면 월~수로 냈던 것을 화~목으로 다시 낸다. 중복 검사는 기간과 수업이
**똑같을 때만** 잡으므로 이 둘은 그냥 통과한다. 화요일과 수요일 수업이 살아 있는 요청
둘에 동시에 들어간다.

승인 관문이 결국 막기는 한다. 그러나 그때까지 일과 담당이 같은 수업을 두 번 풀고,
낸 사람은 아무 말도 못 듣는다. 낼 때 알려 주는 편이 양쪽 모두에게 낫다.

### 14.9 규칙만 옮겨 오고 검사는 안 옮겨 왔다

이 저장소의 CLAUDE.md 에 한국어 문구 규칙이 있다. 그 문서 안에 이렇게 적혀 있다.

> 규칙을 사람이 매번 눈으로 지키지 않는다. 검사를 걸어 둔다. 검사가 없으면 다음에
> 기능을 붙이는 사람이 편의상 옛 방식으로 쓰고, 조용히 되돌아간다.

규칙은 옮겨 왔는데 검사는 안 옮겨 왔다. 적힌 그대로 됐다.

| 어긴 것 | 자리 |
|---|---|
| 해요체 | 10 |
| 낱말 나열 중간점 | 7 |
| em 대시 | 0 |

`scripts/check-copy.mjs` 를 `npm run check` 에 넣었다. 주석은 안 본다. 저장 값과 학교가
실제로 쓰는 이름은 예외로 둔다.

### 14.10 게시된 변경을 원래 표로 되돌려 읽던 네 자리

수업 기록은 학교가 준 원래 표다. 게시된 변경은 그 위에 얹는 덧칠이라 기록 자체는 안
바뀐다. "지금 이 수업의 담당과 자리"를 물을 때마다 덧칠을 얹어 읽어야 하는데, 네 군데가
원래 표를 그대로 읽고 있었다.

| 자리 | 무슨 일이 났나 |
|---|---|
| 영향 수업 고르기 | 보강 맡은 수업이 그 사람 목록에 안 뜸 |
| 사건 만들기 관문 | 그 수업을 담으려 하면 "신청자가 담당이 아니다"로 튕김 |
| 자리 예약 검사 | 한 번 게시된 수업은 그 주 내내 못 움직임 |
| 근무 불가 자리 | 격자와 어긋난 자리를 막아, 결강난 자리로 다시 옮겨짐 |

첫째 줄이 가장 무겁다. 보강을 맡은 사람이 그날 결강을 내면 그 수업이 영향 수업 목록에
안 뜬다. 아무도 다시 안 맡고, **그날 그 학급에 들어갈 사람이 없어진다.** 게시까지
끝난 뒤라 아무도 눈치채지 못한다.

넷째 줄은 실제 자료로 세었다. 게시 71건 가운데 5건이 결강난 자리로 다시 옮겨졌다.
고친 뒤 0건이다.

세 층 가운데 이것을 잡은 것은 실제 자료 시뮬레이션이다. 단위 검사는 통과했고, 스모크는
게시가 한 번뿐인 예시 자료라 안 걸린다. 게시가 **쌓인 뒤**라야 드러난다.

시뮬레이션 자신도 원래 표를 보고 있었다. 화면과 다른 방식으로 수업을 골라서, 실제로
일어나지 않는 상황을 재고 있었다. 같은 표를 보게 맞추고 나서야 이 네 가지가 드러났다.
**재는 도구가 재려는 것과 같은 길을 걷는지 먼저 봐야 한다.**

---

## 15. 남은 것

1. **학교가 내보낸 강좌별 수강 자료.** 나이스 학급 시간표는 (학급, 교시)마다 과목을
   대개 하나만 준다. 한 학급이 여러 강좌로 갈리는 편성은 대체로 눌려서 온다.
   217곳에서 눌리지 않고 오는 자리가 일반고 1.98%, 특성화고 15.81%인 것은 확인했지만
   나머지는 여전히 안 보인다. 이 자료가 있어야 이동수업을 짐작이 아니라 사실로 다룬다.
2. **덩어리 길이 2의 무게.** 일반고의 길이 2가 정말 편성 사고인지, 아니면
   블록타임 편성인지는 이 자료로 못 가른다. 지금은 가볍게 깎는 쪽으로 두었다.
   217곳에서 길이 3 이상이 실재하는 것이 확인됐으니 다시 볼 자리다.
3. **특목고 1학년 오탐 25%.** 과학고와 외국어고 1학년에 계열별 강좌가 실제로 열려
   있어서, 오탐이라 센 것과 맞는 판정이 섞여 있다. 학교 유형을 자료에서 이미 받고 있으니
   유형별로 기준을 나누는 길이 있다. 다만 근거 없이 나누면 값이 둘로 늘 뿐이다.
   특목고 표본을 계열별로 더 모아야 답할 수 있다.
4. **한 주만 보았다.** 217곳 모두 2026년 6월의 한 주다. 고사 기간이나 학기 초처럼
   편성이 다른 주에서 같은 값이 나오는지는 재지 않았다.
5. **진행 중인 주를 학교가 고치면.** 수업 번호가 행 순서라, 같은 주를 다시 부르는
   사이에 학교가 행을 하나 끼우면 뒤 번호가 모두 밀린다. 개정판 번호는 그대로라
   어긋남 검사에 안 걸린다. 14.6절에서 371곳을 재어 보니 지나간 주는 응답이 그대로였다.
   진행 중인 주가 고쳐지는 경우는 못 쟀다. 번호를 내용에서 뽑는 길이 있는데, 번호가
   길어져 저장 한도(5MB)를 밀어 올린다. 재 보고 정할 자리다.
6. **교사 표는 만들어 넣은 것이다.** 나이스에 교사가 없으니 8.8절의 통과 검사는
   학교가 채웠을 법한 표를 지어 넣었다. 실제 학교가 채운 표로 도는 것은 아직 못 보았다.
7. **한 브라우저 안에서만 돈다.** 저장이 `localStorage` 라 교사와 일과 담당이 같은
   기기를 써야 한다. 14.7절에서 넣은 요청 목록도 그 기기 안에서만 보인다. 여러 사람이
   각자 기기에서 쓰려면 저장을 옮겨야 하고, 그것은 이 기록의 범위 밖이다.
8. **초등학교의 보강 쏠림.** 담임 결강의 62%가 교체 상대가 없어 보강으로 간다.
   컴시간알리미가 초등을 안 다루는 것과 같은 자리다. 도구를 초등에 어떻게 맞출지는
   제품 결정이라 여기서 정하지 않는다.

---

## 16. 이 기록을 어떻게 쓰는가

**규칙을 새로 만들거나 기준값을 고칠 때는 여기 방법으로 다시 재고 숫자를 갱신한다.**
합성 학교에서 잘 나온 숫자를 근거로 삼지 않는다. 이번에 그 함정에 한 번 빠졌다.

재는 절차는 넷이다.

1. **인증키를 먼저 받는다.** 키 없이 잰 숫자는 6, 7교시가 없고 한 칸에 과목이 둘인
   자리가 체계적으로 덜 보인다. 발급은 `https://open.neis.go.kr/portal/guide/actKeyPage.do`
   에서 카카오나 네이버 로그인으로 끝난다. 키는 환경 변수로만 두고 저장소에 넣지 않는다.
2. `schoolInfo` 를 교육청 17곳으로 훑어 전국 목록을 만든다. 유형은 `HS_SC_NM` 을 읽고,
   전문계 여부는 `HS_GNRL_BUSNS_SC_NM` 을 읽는다. **이름으로 짐작하지 않는다.**
3. 계열별로 나눈 뒤 무작위로 뽑는다. 이름 조각으로 훑으면 표본이 한쪽으로 쏠린다.
4. `pSize=1000` 과 `pIndex` 로 학교 하나를 통째로 받는다. 알린 행 수와 받은 행 수가
   다르면 그 학교는 버린다. 부분 시간표로는 아무것도 재지 않는다.
5. 통계만 뽑고 원본은 버린다. 학교 자료를 저장소에 남기지 않는다.
6. **표본 전부를 어댑터부터 추천까지 통째로 통과시킨다.** 분포를 재는 것과 자료를
   통과시키는 것은 다른 일이다. 6장의 잘못 둘과 8.7절의 학년 판정 사망이 이 단계에서만
   드러났다.
7. **추천안을 만들어 놓고 끝내지 않는다.** 실제로 적용해 시간표가 여전히 성립하는지
   다시 검사한다. 표 위에서만 맞는 안이 이 도구에서 가장 무서운 실패다.

숫자를 볼 때 지켜야 할 것도 적어 둔다.

- **학년을 나눠 본다.** 학교 전체 평균은 공통과목 학년과 선택과목 학년을 섞어 버린다.
- **학년마다 비슷한 수가 나오는 규칙은 의심한다.** 그 규칙은 학년이 아니라 다른 것을 세고 있다.
- **표본이 셋이면 방향을 보기에는 되고 기준값을 정하기에는 얇다.** 기준값을 바꿀 때는 표본을 늘린다.
- **유형을 나눠 본다.** 결론이 맞아도 이유가 틀릴 수 있다. 이유가 틀리면 그 안내는 못 쓴다.
- **재는 단위와 쓰는 단위를 맞춘다.** 학년으로 쟀는데 학교로 판정하면 잰 뜻이 없다.
  전문교과 몫이 그랬다. 학년으로 재면 특성화고 1학년이 일반고와 구분되지 않는다.
- **"두 무리 사이가 비어 있다"는 표본이 얇다는 뜻일 때가 많다.** 25곳에서 0.00과 0.31
  사이가 비어 보여 그 가운데를 골랐는데, 217곳에서는 그 구간에 13곳이 들어앉았다.
  값은 우연히 살았지만 근거는 죽었다. **맞는 값에 틀린 근거가 붙어 있으면 다음 사람이
  그 근거를 믿고 값을 옮긴다.**
- **기준값이 안 맞으면 값이 아니라 지표를 의심한다.** 2.2를 4.0까지 올려도 오탐이
  안 잡혔다. 분모가 학급 수라 학년이 작으면 무조건 커지는 지표였다. 어느 값을 골라도
  그 성질은 안 없어진다.
- **표본을 반으로 갈라 다시 골라 본다.** 한 표본에서 가장 좋은 값은 그 표본에만 좋을 수
  있다. 스무 번 갈라 고르게 해서 나온 구간 안에 있는 값을 쓴다.
- **시험 자료가 실제 자료와 같은 모양인지 본다.** 학년 판정이 실제 자료 217곳 전부에서
  죽어 있었는데 시험은 전부 통과했다. 시험이 학급 이름을 `1-3` 으로 쓰고 실제 자료는
  학교 코드로 시작하는 JSON 을 쓴다. **모양이 다르면 시험은 아무것도 안 지킨다.**
- **재는 방식이 재려는 것을 가리지 않는지 본다.** 학급 하루를 5행으로 받으면 한 칸에
  과목이 둘인 자리가 행을 둘 먹어 뒤가 잘린다. 그 방식으로 복수 과목 칸의 비율을 재고
  "0%"라고 적었다. 도구가 가린 것을 현실이라 읽은 것이다.
- **자료가 두꺼울 때 정확해지고 얇을 때도 도는 규칙을 고른다.** 절대 개수로 자르면
  한 학급만 받아 온 자료에서 무너진다. 관측된 것 전부와 견주면 양쪽이 다 된다.
- **다 채운 상태만 재지 않는다.** 사람은 아는 만큼만 채우고 쓰기 시작한다.
  덜 채운 상태에서 무엇이 나오는지를 따로 재야 한다. 여기서는 그때 나오는 안이 다 틀렸다.
- **모르는 것을 없는 것으로 바꾸지 않는다.** 담당을 모르는 수업을 빼 버리면
  그 자리가 빈 시간이 된다. 모른다는 사실 자체를 들고 가야 한다.

---

## 참고

- 나이스 교육정보 개방 포털 <https://open.neis.go.kr/>
- 고교학점제 시행 현황과 편성 구조는 03-domain-and-data.md 에 정리했다
